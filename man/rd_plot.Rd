% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-plot.R
\name{rd_plot}
\alias{rd_plot}
\title{Regression discontinuity plot}
\usage{
rd_plot(
  data,
  y,
  x,
  cutoff = 0,
  bins = c("qs", "es"),
  n_bins = NULL,
  p = 4,
  fit = c("polynomial", "local", "none"),
  h = NULL,
  kernel = "triangular",
  ci = FALSE,
  weights = NULL,
  x_range = NULL,
  colours = c("#0072B2", "#D55E00"),
  point_size = 1.6,
  x_lab = x,
  y_lab = y
)
}
\arguments{
\item{data}{A data frame.}

\item{y, x}{Outcome and running variable column names.}

\item{cutoff}{Cutoff value.}

\item{bins}{\code{"qs"} (quantile-spaced, default) or \code{"es"} (evenly spaced).}

\item{n_bins}{Number of bins per side: one number, a vector \code{c(left, right)}, or \code{NULL} to use the integrated-mean-squared-error optimal
number of \code{rdrobust::rdplot()} (mimicking-variance variant) when that
package is installed, and 20 per side otherwise.}

\item{p}{Polynomial order of the global fit (default 4).}

\item{fit}{\code{"polynomial"} (default), \code{"local"} (local linear within \code{h}),
or \code{"none"}.}

\item{h}{Bandwidth for \code{fit = "local"}; \code{NULL} uses the MSE-optimal
bandwidth from \code{rdrobust::rdbwselect()}.}

\item{kernel}{Kernel for the local fit.}

\item{ci}{Draw pointwise 95 percent bars around the bin means.}

\item{weights}{Optional weights column name.}

\item{x_range}{Optional range of \code{x} to display.}

\item{colours}{Two colours for the left and right sides.}

\item{point_size}{Size of the bin-mean points.}

\item{x_lab, y_lab}{Axis labels.}
}
\value{
A ggplot object with attributes \code{"rd_bins"} (the binned data)
and \code{"rd_fit"} (the fitted curves).
}
\description{
Binned means of the outcome on each side of the cutoff with a fitted
curve per side, as a ggplot object that can be modified with the usual
\code{+} layers. Bin counts default to the integrated-mean-squared-error
optimal choice of Calonico, Cattaneo, and Titiunik (2015) through
\code{rdrobust::rdplot()} when installed. The fitted curve is a global
polynomial of order \code{p} on each side (the \code{rdplot} convention) or a
local linear fit within bandwidth \code{h} (the estimator's own view of the
data).
}
\examples{
dat <- sim_rd(2000, "lee", seed = 1)
rd_plot(dat, "y", "x")
rd_plot(dat, "y", "x", fit = "local", ci = TRUE) + ggplot2::labs(title = "Local linear fit")
}
\references{
Calonico, S., Cattaneo, M. D., and Titiunik, R. (2015). Optimal
data-driven regression discontinuity plots. \emph{Journal of the American
Statistical Association}, 110(512), 1753-1769.
}
