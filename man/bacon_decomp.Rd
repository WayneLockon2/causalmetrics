% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twfe-diagnostics.R
\name{bacon_decomp}
\alias{bacon_decomp}
\title{Goodman-Bacon decomposition of the static two-way fixed effects estimator}
\usage{
bacon_decomp(data, id, time, y, d)
}
\arguments{
\item{data}{A balanced panel in long format.}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period.}

\item{y}{Column name of the outcome.}

\item{d}{Column name of the treatment indicator (0/1, absorbing: once 1,
stays 1). Units treated in the first period are always treated and are
dropped with a warning, as in the original decomposition.}
}
\value{
An object of class \code{cm_bacon}: \code{decomposition} (one row per
comparison: \code{treated}, \code{control}, \code{type}, \code{weight}, \code{estimate}),
\code{by_type} (weights and weighted averages by comparison type),
\code{twfe} (the static TWFE coefficient), and \code{check} (the weighted sum of
the two-by-two estimates, which equals \code{twfe} in a balanced panel).
}
\description{
With staggered adoption, the coefficient on the treatment dummy in
\code{y ~ d | id + time} is a weighted average of all two-by-two
difference-in-differences comparisons in the data: each timing group
against the never-treated units, earlier-treated against later-treated
units (before the later group is treated), and later-treated against
earlier-treated units (after the earlier group is treated). The last type
uses already-treated units as controls, which is where heterogeneous
dynamic effects bias the regression. \code{bacon_decomp()} returns every
comparison, its estimate, and its weight (Goodman-Bacon 2021).
}
\examples{
dat <- sim_did_panel(n_units = 200, n_periods = 8, groups = c(3, 6), seed = 1)
bacon_decomp(dat, id = "id", time = "time", y = "y", d = "treated")
}
\references{
Goodman-Bacon, A. (2021). Difference-in-differences with
variation in treatment timing. \emph{Journal of Econometrics}, 225(2), 254-277.
}
