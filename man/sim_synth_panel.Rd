% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-synth-panel.R
\name{sim_synth_panel}
\alias{sim_synth_panel}
\title{Simulate a panel from a linear factor model for synthetic control}
\usage{
sim_synth_panel(
  n_donors = 20,
  n_treated = 1,
  t_pre = 20,
  t_post = 5,
  n_factors = 2,
  sd_e = 1,
  sd_factor = 1,
  rho = 0.5,
  trend = 0.2,
  select_loading = 0,
  select_level = 0,
  factor_shift = 0,
  effect = 0,
  adoption = NULL,
  seed = NULL
)
}
\arguments{
\item{n_donors, n_treated}{Numbers of donor and treated units.}

\item{t_pre, t_post}{Numbers of pre- and post-treatment periods for a unit
adopting at \code{t_pre + 1}.}

\item{n_factors}{Number of common factors.}

\item{sd_e}{Standard deviation of the idiosyncratic shock; small values
give a near-perfect pre-treatment fit.}

\item{sd_factor, rho}{Standard deviation and AR(1) coefficient of the
factors.}

\item{trend}{Slope of the common trend \code{delta_t = trend * t}.}

\item{select_loading}{Loading of the treated unit(s) on the first factor,
in standard deviations of the donor loadings (0 keeps it random).}

\item{select_level}{Unit effect of the treated unit(s), in standard
deviations of the donor effects (0 keeps it random).}

\item{factor_shift}{Mean shift of the first factor in the post-treatment
periods (a "break", Ferman and Pinto Table 1 Panels C-D); with a
nonzero shift and selection on the loading, DiD and the demeaned
synthetic control are both biased.}

\item{effect}{Treatment effect: a scalar or a vector over event times
\verb{0, 1, ...} (recycled).}

\item{adoption}{Optional vector of adoption periods for the treated units
(staggered adoption); default all adopt at \code{t_pre + 1}.}

\item{seed}{Optional seed.}
}
\value{
A data frame with \code{id}, \code{time}, \code{y}, \code{d}, \code{y0} (untreated
potential outcome), and \code{tau} (the effect in treated cells), with
attributes \code{factors}, \code{loadings}, and \code{unit_effects}.
}
\description{
Potential outcomes follow the linear factor model of Ferman and Pinto
(2021): \verb{y_jt = c_j + delta_t + lambda_t' mu_j + e_jt}, with unit effects
\code{c_j}, a common trend \code{delta_t}, common factors \code{lambda_t} (stationary
AR(1) processes), unit loadings \code{mu_j}, and idiosyncratic shocks. Options
place the treated unit in the tail of the loading or of the unit-effect
distribution (selection on unobservables), shift the first factor after
treatment, and stagger adoption across several treated units.
}
\examples{
dat <- sim_synth_panel(n_donors = 20, t_pre = 20, t_post = 5, select_loading = 1.5,
                       factor_shift = 1, effect = 2, seed = 1)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d", demean = TRUE)
fit$estimate
}
