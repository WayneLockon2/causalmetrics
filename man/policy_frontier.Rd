% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/policy.R
\name{policy_frontier}
\alias{policy_frontier}
\title{Targeting frontier between impact and deprivation}
\usage{
policy_frontier(
  scores,
  tau_hat,
  y0_hat = NULL,
  budget = 0.3,
  utility = c("cara", "crra"),
  curvature = c(0, 0.5, 1, 2, 5),
  cost = 0,
  conf_level = 0.95
)
}
\arguments{
\item{scores}{A \code{cm_scores} object (test sample).}

\item{tau_hat}{Predicted effects: a \code{cm_cate} object, a numeric vector,
or a column name.}

\item{y0_hat}{Predicted untreated outcomes (default the cross-fitted
\code{mu0} of \code{scores}).}

\item{budget}{Share of units to treat.}

\item{utility}{\code{"cara"} (\verb{u(y) = (1 - exp(-a y)) / a}, linear at \code{a = 0})
or \code{"crra"} (\code{u(y) = (y^(1 - r) - 1) / (1 - r)}, \code{log(y)} at \code{r = 1},
requires positive outcomes).}

\item{curvature}{Numeric vector of curvature parameters.}

\item{cost}{Cost per treated unit for the value calculation.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{cm_frontier} with \code{table} (curvature, overlap
with impact-only and deprivation-only targeting, mean predicted effect
and untreated outcome of the selected units, doubly robust value and
standard error), \code{selected} (logical matrix, one column per curvature),
\code{impact_only}, \code{deprivation_only}, and the inputs.
\code{\link[=plot_policy_frontier]{plot_policy_frontier()}} draws the selected region.
}
\description{
Implements the targeting exercise of Haushofer, Niehaus, Paramo, Miguel,
and Walker (2025): a planner with utility \code{u} allocates a transfer to the
\code{budget} share of units with the largest predicted utility gain
\code{u(y0 + tau) - u(y0)}, where \code{y0} is the predicted untreated outcome
(deprivation) and \code{tau} the predicted effect. With linear utility the
planner targets impact only; with strong curvature it targets deprivation.
For each curvature value the function reports the overlap of the chosen set
with impact-only and deprivation-only targeting, and the doubly robust
value of the rule.
}
\references{
Haushofer, J., Niehaus, P., Paramo, C., Miguel, E., and Walker, M. (2025).
Targeting impact versus deprivation. \emph{American Economic Review}.
}
