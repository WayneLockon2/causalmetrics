% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediate-cde.R
\name{mediate_cde}
\alias{mediate_cde}
\title{Controlled direct effect by sequential g-estimation}
\usage{
mediate_cde(
  data,
  y,
  d,
  m,
  x_pre = NULL,
  x_post = NULL,
  m_ref = 0,
  interaction = FALSE,
  cluster = NULL,
  n_boot = 499L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d, m}{Outcome, binary treatment, and mediator column names.}

\item{x_pre}{Pre-treatment covariates.}

\item{x_post}{Post-treatment covariates that confound the mediator and the
outcome (intermediate confounders).}

\item{m_ref}{Mediator level for the controlled direct effect.}

\item{interaction}{Allow a \code{D:M} interaction in the first step (the
demediation then uses \code{delta + delta_int * D}).}

\item{cluster}{Optional cluster column for the bootstrap.}

\item{n_boot}{Bootstrap replications.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_med_cde} with \code{effects} (\code{cde} and the naive
single-regression coefficient on \code{D} for comparison), \code{delta} (the
mediator effect from step 1), the two fitted models, and settings.
}
\description{
Estimates the controlled direct effect \code{E[Y(1, m) - Y(0, m)]} of a binary
treatment holding the mediator at \code{m_ref}, when some confounders of the
mediator-outcome relationship (\code{x_post}) are affected by the treatment, so
that conditioning on them in one regression would bias the treatment
coefficient (they are post-treatment) and omitting them would bias the
mediator coefficient. The two-step procedure of Acharya, Blackwell, and
Sen (2016):
}
\details{
\enumerate{
\item regress \code{Y} on \code{D}, \code{M}, \code{x_pre}, and \code{x_post} (with \code{D:M} when
\code{interaction = TRUE}), and estimate the mediator's effect \code{delta};
\item form the demediated outcome \code{Y - delta * (M - m_ref)};
\item regress the demediated outcome on \code{D} and \code{x_pre} only; the coefficient
on \code{D} is the controlled direct effect.
}

Standard errors come from a bootstrap of both steps (clustered when
\code{cluster} is given), which accounts for the estimated \code{delta}.
}
\examples{
dat <- sim_mediation(1500, dgp = "post_treatment_confounder", seed = 1)
fit <- mediate_cde(dat, "y", "d", "m", x_pre = c("x1", "x2"), x_post = "z", n_boot = 99, seed = 1)
fit
attr(dat, "truth")$cde
}
\references{
Acharya, A., Blackwell, M., and Sen, M. (2016). Explaining causal findings
without bias: detecting and assessing direct effects. \emph{American Political
Science Review}, 110(3), 512-529.
}
