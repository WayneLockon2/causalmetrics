% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-mediation.R
\name{sim_mediation}
\alias{sim_mediation}
\title{Simulate data for mediation analysis}
\usage{
sim_mediation(
  n = 1000L,
  dgp = c("linear", "interaction", "nonlinear", "binary", "correlated_errors",
    "post_treatment_confounder", "iv_mediator", "front_door", "parallel"),
  rho = 0.5,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{dgp}{Design:
\itemize{
\item \code{"linear"}: two covariates, confounded treatment, linear mediator and
outcome models without treatment-mediator interaction; product of
coefficients is exact.
\item \code{"interaction"}: as \code{"linear"} with a treatment-mediator interaction, so
pure and total direct effects differ.
\item \code{"nonlinear"}: nonlinear covariate effects in the treatment, mediator,
and outcome models (the case for machine-learning nuisances).
\item \code{"binary"}: binary mediator and binary outcome (logit models).
\item \code{"correlated_errors"}: \code{"linear"} with correlated mediator and outcome
errors of correlation \code{rho} (an unmeasured mediator-outcome confounder);
sequential ignorability fails and the truth records \code{rho}.
\item \code{"post_treatment_confounder"}: a covariate \code{z} affected by the treatment
confounds the mediator and the outcome; natural effects are not
identified, the controlled direct effect is.
\item \code{"iv_mediator"}: an unobserved \code{u} drives both the mediator and the
outcome; an instrument \code{z} shifts the mediator; treatment randomized.
\item \code{"front_door"}: an unobserved \code{u} confounds treatment and outcome; the
mediator is unconfounded given the treatment and carries the whole
effect.
\item \code{"parallel"}: two mediators \code{m1}, \code{m2} with different treatment effects.
}}

\item{rho}{Error correlation for \code{"correlated_errors"}.}

\item{seed}{Optional seed.}
}
\value{
A data frame with \code{y}, \code{d}, mediator column(s) \code{m} (or \code{m1}, \code{m2}),
covariates \code{x1}, \code{x2} (plus \code{z} or \code{u} where the design has them), and the
attribute \code{"truth"}, a list with \code{total}, \code{nde}, \code{nie}, \code{nde_total},
\code{nie_pure}, \code{cde} (at the mediator reference stored in \code{m_ref}), and
design-specific entries.
}
\description{
Designs with a binary treatment \code{d}, one or more mediators, and an outcome,
with the true total, natural direct, natural indirect, and controlled direct
effects attached as \code{attr(, "truth")} (finite-sample averages of the
simulated potential outcomes).
}
\examples{
dat <- sim_mediation(500, dgp = "linear", seed = 1)
attr(dat, "truth")[c("total", "nde", "nie")]
}
