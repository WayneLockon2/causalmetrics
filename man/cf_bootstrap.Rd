% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-function.R
\name{cf_bootstrap}
\alias{cf_bootstrap}
\title{Bootstrap inference for a two-step control-function estimator}
\usage{
cf_bootstrap(
  data,
  first,
  second,
  n_boot = 499L,
  cluster = NULL,
  seed = NULL,
  ape = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{first}{A function of a data frame returning the data with the
control added (typically a call to \code{\link[=cf_residuals]{cf_residuals()}}).}

\item{second}{A function of the augmented data returning a fitted model
(anything with a \code{coef()} method) or a named numeric vector.}

\item{n_boot}{Bootstrap replications.}

\item{cluster}{Optional cluster column name for a cluster bootstrap.}

\item{seed}{Optional seed.}

\item{ape}{Optional function of \verb{(fit, data)} returning a named numeric
vector of average partial effects (for instance a call to \code{\link[=cf_ape]{cf_ape()}}).}

\item{conf_level}{Confidence level for the percentile intervals.}
}
\value{
A list of class \code{cm_cf_boot} with \code{table} (term, estimate,
std.error, conf.low, conf.high, component), the matrix of bootstrap
draws, and the number of failed replications.
}
\description{
Re-runs both stages on bootstrap samples (pairs bootstrap, or cluster
bootstrap when \code{cluster} is given) and reports bootstrap standard errors
and percentile intervals for the second-stage coefficients and, if
requested, for average partial effects. Second-stage standard errors that
treat the control as a fixed regressor are wrong (generated-regressor
problem); the bootstrap is the simplest valid alternative.
}
\examples{
dat <- sim_iv(600, dgp = "probit_cf", seed = 1)
first <- function(df) cf_residuals(df, d = "d", z = "z", x = "x1")
second <- function(df) glm(y ~ d + x1 + v_hat, data = df, family = binomial("probit"))
cf_bootstrap(dat, first, second, n_boot = 30, seed = 1,
             ape = function(fit, df) cf_ape(fit, df, d = "d"))
}
