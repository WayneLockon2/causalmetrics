% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr-scores.R
\name{dr_scores}
\alias{dr_scores}
\title{Cross-fitted doubly robust pseudo-outcomes}
\usage{
dr_scores(
  data,
  y,
  d,
  x = NULL,
  p_hat = NULL,
  mu0_hat = NULL,
  mu1_hat = NULL,
  learner_p = NULL,
  learner_mu = NULL,
  learner_mu0 = NULL,
  learner_mu1 = NULL,
  type = c("dr", "ipw", "reg"),
  folds = 5L,
  fold_id = NULL,
  seed = NULL,
  p_clip = c(0.01, 0.99),
  outcome_type = c("auto", "continuous", "binary"),
  na_action = c("fail", "omit")
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d}{Outcome and binary treatment column names.}

\item{x}{Character vector of covariate names (the controls \code{Z}). Needed
when the nuisances are estimated internally, and used as the default
heterogeneity variables downstream.}

\item{p_hat, mu0_hat, mu1_hat}{Optional out-of-fold predictions of
\code{P(D = 1 | Z)}, \verb{E[Y | D = 0, Z]}, \verb{E[Y | D = 1, Z]}: numeric vectors of
length \code{nrow(data)} or column names. When all three are supplied no
learner is fitted.}

\item{learner_p}{\code{mlr3} classification learner for the propensity score
(default logistic regression).}

\item{learner_mu}{\code{mlr3} regression learner for the two outcome regressions
(default linear regression); \code{learner_mu0}, \code{learner_mu1} override it
arm by arm.}

\item{learner_mu0, learner_mu1}{Optional arm-specific learners.}

\item{type}{\code{"dr"} (default) for the doubly robust signal, \code{"ipw"} for
\verb{Y H(p)}, \code{"reg"} for \code{mu1 - mu0}. The alternatives exist to show why
the doubly robust signal is preferred.}

\item{folds}{Number of cross-fitting folds.}

\item{fold_id}{Optional fold identifier (vector or column name) recording
the folds of supplied nuisances or imposing a partition.}

\item{seed}{Optional seed for the fold partition.}

\item{p_clip}{Propensity clipping bounds.}

\item{outcome_type}{\code{"auto"}, \code{"continuous"}, or \code{"binary"}; matters only
when a classification learner predicts the outcome.}

\item{na_action}{\code{"fail"} or \code{"omit"} for rows with missing values.}
}
\value{
A list of class \code{cm_scores}: \code{score} (the pseudo-outcome),
\code{nuisance} (data frame with \code{p}, \code{mu0}, \code{mu1}, \code{l}), \code{residuals}
(\code{y_tilde}, \code{d_tilde}), \code{y}, \code{d}, \code{x} (covariate names), \code{data} (the
retained rows), \code{fold_id}, \code{ate} (estimate and standard error),
\code{diagnostics}, and learner labels. \code{\link[generics:tidy]{tidy()}} returns the ATE row.
}
\description{
\code{dr_scores()} builds, for every observation, the doubly robust (AIPW)
signal
\deqn{Y_i(\hat\eta) = \hat\mu_1(Z_i) - \hat\mu_0(Z_i) + H_i(\hat p)\,(Y_i - \hat\mu_{D_i}(Z_i)),
\qquad H_i(p) = \frac{D_i}{p(Z_i)} - \frac{1 - D_i}{1 - p(Z_i)},}
whose conditional mean given any function of the covariates is the
conditional average treatment effect, \code{E[Y(eta0) | X] = tau(X)}, under
conditional exogeneity. Its sample mean is the AIPW estimate of the ATE.
The same vector is the regression label of the DR-learner
(\code{\link[=cate_learner]{cate_learner()}}), the outcome of best linear predictors
(\code{\link[=cate_blp]{cate_blp()}}, \code{\link[=cate_gate]{cate_gate()}}), the loss of model selection
(\code{\link[=cate_score]{cate_score()}}, \code{\link[=cate_ensemble]{cate_ensemble()}}), the input of every validation
statistic (\code{\link[=cate_validate]{cate_validate()}}), and the reward of policy evaluation and
learning (\code{\link[=policy_value]{policy_value()}}, \code{\link[=policy_learn]{policy_learn()}}).
}
\details{
Nuisances are cross-fitted with \code{mlr3} learners (as in \code{\link[=est_dml]{est_dml()}}) or
supplied as out-of-fold predictions. The object also stores the residuals
\code{Y - l(Z)} and \code{D - p(Z)}, with \verb{l(Z) = p mu1 + (1 - p) mu0}, for the
R-learner.
}
\examples{
dat <- sim_hte(n = 600, dgp = "smooth", seed = 1)
sc <- dr_scores(dat, y = "y", d = "d", x = paste0("x", 1:5), seed = 1)
sc
# the mean of the score is the AIPW estimate of the ATE
tidy(sc)
}
\references{
Chernozhukov, V., Hansen, C., Kallus, N., Spindler, M., and Syrgkanis, V.
(2026). \emph{Applied Causal Inference Powered by ML and AI}, chapters 14-15.

Kennedy, E. H. (2023). Towards optimal doubly robust estimation of
heterogeneous causal effects. \emph{Electronic Journal of Statistics}, 17(2).
}
\seealso{
\code{\link[=cate_learner]{cate_learner()}}, \code{\link[=cate_blp]{cate_blp()}}, \code{\link[=cate_validate]{cate_validate()}}, \code{\link[=policy_learn]{policy_learn()}}
}
