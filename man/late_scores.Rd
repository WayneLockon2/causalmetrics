% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv-compliers.R
\name{late_scores}
\alias{late_scores}
\title{LATE score components as pseudo-outcomes}
\usage{
late_scores(fit)
}
\arguments{
\item{fit}{A \code{cm_dml} object with \code{model = "iivm"}.}
}
\value{
A list with \code{outcome} and \code{treatment} (both \code{cm_scores}), the
LATE \code{estimate}, and the covariate names.
}
\description{
Extracts from an interactive-IV \code{est_dml()} fit the two cross-fitted
scores whose ratio is the LATE: \code{psi_b}, the doubly robust effect of the
instrument on the outcome, and \code{psi_a}, the doubly robust effect of the
instrument on the treatment (complier indicator in expectation). Both are
returned as \code{cm_scores} objects so that the heterogeneity tools of the
package (\code{\link[=cate_blp]{cate_blp()}}, \code{\link[=cate_gate]{cate_gate()}}, \code{\link[=cate_learner]{cate_learner()}}, \code{\link[=policy_learn]{policy_learn()}})
apply to compliers: a rule with value \verb{mean(pi(X) (psi_b - c psi_a))}
measures gains among compliers net of cost \code{c}.
}
\seealso{
\code{\link[=late_blp]{late_blp()}}
}
