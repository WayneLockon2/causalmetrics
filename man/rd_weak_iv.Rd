% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-weak-iv.R
\name{rd_weak_iv}
\alias{rd_weak_iv}
\title{Weak-identification-robust inference for fuzzy RD}
\usage{
rd_weak_iv(
  data,
  y,
  d,
  x,
  cutoff = 0,
  h = NULL,
  kernel = "triangular",
  covariates = NULL,
  cluster = NULL,
  conf_level = 0.95,
  compare = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d, x}{Outcome, treatment, and running variable column names.}

\item{cutoff}{Cutoff value.}

\item{h}{Bandwidth; \code{NULL} uses the fuzzy MSE-optimal bandwidth of
\code{rdrobust::rdbwselect()}.}

\item{kernel}{Kernel for the local weights.}

\item{covariates}{Optional covariate column names added as controls.}

\item{cluster}{Optional cluster column name.}

\item{conf_level}{Confidence level.}

\item{compare}{Also run \code{rdrobust::rdrobust(fuzzy = )} at the same
bandwidth and report its rows.}
}
\value{
A list of class \code{cm_rd_weak_iv} with \code{ar_set} (the
\code{\link[=iv_ar_confidence_set]{iv_ar_confidence_set()}} object), \code{first_stage} (jump, F, robust F,
effective F), \code{table} (rows: local 2SLS with the Wald interval, the
Anderson-Rubin set, and the \code{rdrobust} rows when \code{compare = TRUE}),
\code{h}, \code{n_eff}.
}
\description{
A fuzzy RD is a local instrumental-variables problem: the indicator of
being above the cutoff instruments the treatment, with the running
variable (and its interaction with the indicator) as controls, on the
observations inside the bandwidth weighted by the kernel. When the
first-stage jump is small the Wald interval of the ratio is unreliable
(Feir, Lemieux, and Marmer 2016). This function forms the local
two-stage least squares estimate, the local first-stage F statistics,
and the Anderson-Rubin confidence set, which stays valid however weak
the first stage is, by calling \code{\link[=iv_ar_confidence_set]{iv_ar_confidence_set()}} on the local
sample with kernel weights.
}
\examples{
dat <- sim_rd(3000, "fuzzy", compliance = 0.7, seed = 1)
rd_weak_iv(dat, "y", "d", "x")
}
\references{
Feir, D., Lemieux, T., and Marmer, V. (2016). Weak identification in
fuzzy regression discontinuity designs. \emph{Journal of Business and Economic
Statistics}, 34(2), 185-196.
}
