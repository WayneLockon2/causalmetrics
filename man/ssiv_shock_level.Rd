% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssiv.R
\name{ssiv_shock_level}
\alias{ssiv_shock_level}
\title{Shock-level regression for a shift-share instrument}
\usage{
ssiv_shock_level(
  data,
  y,
  d,
  shares,
  shocks,
  x = NULL,
  weights = NULL,
  cluster = NULL
)
}
\arguments{
\item{data}{A data frame with one row per unit.}

\item{y, d}{Outcome and treatment column names.}

\item{shares}{Character vector of share column names (one per shock).}

\item{shocks}{Numeric vector of shocks, in the order of \code{shares}.}

\item{x}{Optional control column names.}

\item{weights}{Optional weights column name.}

\item{cluster}{Optional vector (length of \code{shares}) assigning shocks to
clusters for the shock-level standard errors.}
}
\value{
A list of class \code{cm_ssiv_shock} with \code{estimate}, \code{std.error}
(heteroskedasticity-robust at the shock level), \code{unit_level} (the
unit-level 2SLS estimate, which should coincide), the shock-level
\code{table}, and the number of shocks.
}
\description{
Borusyak, Hull, and Jaravel (2022) show that the shift-share IV estimate
equals a shock-level instrumental-variables regression: aggregate the
residualized outcome and treatment to the shock level with exposure
weights \verb{s_k = sum_i w_i s_ik}, \verb{ybar_k = sum_i w_i s_ik y_i / s_k}, and
regress \code{ybar_k} on \code{dbar_k} instrumented by \code{g_k} with weights \code{s_k}
(no intercept when shares sum to one; otherwise the sum of shares is
added to the controls). Inference at the shock level is valid when the
shocks are as good as randomly assigned, which is the exogenous-shocks
identification story.
}
\examples{
dat <- sim_iv(400, dgp = "shift_share", n_industries = 10, seed = 1)
ssiv_shock_level(dat, "y", "d", shares = paste0("s", 1:10), shocks = attr(dat, "shocks"), x = "x1")
}
\references{
Borusyak, K., Hull, P., and Jaravel, X. (2022). Quasi-experimental
shift-share research designs. \emph{Review of Economic Studies}, 89(1),
181-213.
}
