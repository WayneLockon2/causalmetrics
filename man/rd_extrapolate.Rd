% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-extrapolate.R
\name{rd_extrapolate}
\alias{rd_extrapolate}
\title{Extrapolate regression discontinuity effects away from the cutoff}
\usage{
rd_extrapolate(
  data,
  y,
  x,
  cutoff = 0,
  covariates,
  window = NULL,
  method = c("regression", "aipw"),
  learner = NULL,
  learner_p = NULL,
  target = NULL,
  curve_bandwidth = NULL,
  folds = 5L,
  seed = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{y, x}{Outcome and running variable column names.}

\item{cutoff}{Cutoff value.}

\item{covariates}{Character vector of covariate names (\code{Z}).}

\item{window}{Half-width of the window around the cutoff; \code{NULL} uses the
MSE-optimal bandwidth of \code{rdrobust::rdbwselect()} when installed and
the interquartile range of \code{x - cutoff} otherwise.}

\item{method}{\code{"regression"} (default) or \code{"aipw"}.}

\item{learner}{Optional \code{mlr3} regression learner for the outcome
regressions (default linear regression).}

\item{learner_p}{Optional \code{mlr3} classification learner for the side
propensity (\code{method = "aipw"}).}

\item{target}{Grid of running-variable values for the effect curve;
\code{NULL} uses ten points per side inside the window.}

\item{curve_bandwidth}{Bandwidth of the local linear curve (default half
the window).}

\item{folds, seed}{Cross-fitting settings for \code{method = "aipw"}.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{cm_rd_extrapolate} with \code{test} (per side: Wald
statistic, df, p-value), \code{effects} (rows \code{above} and \code{below}: estimate,
std.error, conf.low, conf.high, n), \code{curve} (x, estimate, std.error,
conf.low, conf.high, side), \code{window}, \code{method}.
}
\description{
The RD estimand is the effect at the cutoff. Angrist and Rokkanen (2015)
extrapolate under a conditional independence assumption (CIA): within a
window around the cutoff, potential outcomes are mean independent of the
running variable given covariates \code{Z}. Two consequences follow.
}
\details{
\itemize{
\item \strong{A test.} On each side of the cutoff, the outcome should not depend
on the running variable once \code{Z} is controlled for; the function
reports the Wald test of the running-variable terms in a regression of
\code{y} on \code{x}, \code{x^2}, and \code{Z} per side, with HC1 standard errors.
\item \strong{Extrapolation.} The untreated mean of units above the cutoff is
\code{E[mu_0(Z) | above]} with \code{mu_0} fitted below, so the average effect on
the treated side is \code{mean(y - mu_0_hat(Z))} over units above the
cutoff, and symmetrically for the average effect on the untreated
side. \code{method = "aipw"} estimates the same objects doubly robustly
with \code{\link[=est_aipw]{est_aipw()}} using the side indicator as the treatment; \code{method = "regression"} uses outcome regressions only. A curve of effects along
the running variable is obtained by a kernel-weighted local linear
regression of the residual \code{y - mu_other(Z)} on \code{x} at a grid of
points.
}
}
\examples{
dat <- sim_rd(3000, "cia", seed = 1)
ex <- rd_extrapolate(dat, "y", "x", covariates = "z", window = 0.5)
ex
}
\references{
Angrist, J. D. and Rokkanen, M. (2015). Wanna get away? Regression
discontinuity estimation of exam school effects away from the cutoff.
\emph{Journal of the American Statistical Association}, 110(512), 1331-1344.
}
