% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-study.R
\name{event_study_frame}
\alias{event_study_frame}
\title{Put event-study estimates from different estimators on one frame}
\usage{
event_study_frame(..., ref = -1, conf_level = 0.95)
}
\arguments{
\item{...}{Named objects to combine; the names become the \code{method}
column. Data frames must have columns \code{event_time}, \code{estimate},
\code{std.error}.}

\item{ref}{Event time(s) omitted as the reference in the regressions
(default \code{-1}), added as zero rows.}

\item{conf_level}{Confidence level for pointwise intervals computed from
standard errors.}
}
\value{
A data frame with \code{method}, \code{event_time}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, \code{band.low}, \code{band.high} (uniform band when the
source provides one, otherwise \code{NA}).
}
\description{
\code{event_study_frame()} collects event-time estimates from
\code{\link[=aggregate_att]{aggregate_att()}} (dynamic type), \code{\link[=att_gt]{att_gt()}} with a single cohort,
\code{\link[=did_imputation]{did_imputation()}}, \code{fixest} regressions with \code{i(event_time, ref = ...)} or
\code{sunab()} terms, or plain data frames, and returns one tidy table that
\code{\link[=plot_event_study]{plot_event_study()}} can draw. Reference periods omitted from a regression
are added back as zeros.
}
\examples{
dat <- sim_did_panel(n_units = 300, n_periods = 8, groups = c(3, 6), seed = 1)
cs <- aggregate_att(att_gt(dat, "id", "time", "g", "y", n_boot = 99), type = "dynamic")
dat$rel <- ifelse(dat$g > 0, dat$time - dat$g, -1000)
tw <- fixest::feols(y ~ i(rel, ref = c(-1, -1000)) | id + time, data = dat, cluster = ~id)
frame <- event_study_frame("Callaway-Sant'Anna" = cs, "TWFE event study" = tw)
plot_event_study(frame)
}
