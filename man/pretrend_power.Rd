% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pretrend-power.R
\name{pretrend_power}
\alias{pretrend_power}
\title{Power of a pre-trends test and the bias conditional on passing it}
\usage{
pretrend_power(
  estimate,
  vcov = NULL,
  event_time = NULL,
  ref = -1,
  slope = NULL,
  target_power = c(0.5, 0.8),
  alpha = 0.05,
  n_sim = 5000L,
  seed = NULL
)
}
\arguments{
\item{estimate}{Numeric vector of event-study coefficients, or an object
from \code{\link[=aggregate_att]{aggregate_att()}} with \code{type = "dynamic"}.}

\item{vcov}{Covariance matrix of the coefficients (ignored when
\code{estimate} is an \code{aggregate_att()} object, whose influence functions
supply it).}

\item{event_time}{Event times of the coefficients.}

\item{ref}{Reference event time through which the linear trend passes.}

\item{slope}{Slope(s) of the hypothesized linear violation, in outcome
units per period. \code{NULL} reports only the slopes detected with the
target powers.}

\item{target_power}{Powers for which to solve the detectable slope.}

\item{alpha}{Significance level of the pre-test.}

\item{n_sim}{Number of simulation draws.}

\item{seed}{Optional seed.}
}
\value{
A list with \code{slopes} (for each requested slope: power of the
pre-test, and for each post-treatment event time the unconditional and
conditional bias), \code{detectable} (slopes detected with the target powers),
and the inputs.
}
\description{
Roth (2022) shows that an event-study pre-test has low power against
violations of parallel trends that would badly bias the post-treatment
estimates, and that conditioning on passing the test makes the bias
worse. \code{pretrend_power()} quantifies both for a linear violation: given
event-study coefficients, their covariance, and a hypothesized slope, it
simulates the estimates under the trend, computes the probability that
the standard pre-test rejects (any pre-treatment coefficient significant
at level \code{alpha}), and reports the bias of each post-treatment coefficient
unconditionally and conditional on not rejecting. It also solves for the
slope that the pre-test would detect with a given power.
}
\references{
Roth, J. (2022). Pretest with caution: Event-study estimates
after testing for parallel trends. \emph{American Economic Review: Insights},
4(3), 305-322.
}
