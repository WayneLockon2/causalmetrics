% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate-blp.R
\name{cate_blp}
\alias{cate_blp}
\title{Best linear predictor of the CATE}
\usage{
cate_blp(
  scores,
  formula = NULL,
  data = NULL,
  conf_level = 0.95,
  uniform = TRUE,
  n_boot = 999L,
  seed = NULL
)
}
\arguments{
\item{scores}{A \code{cm_scores} object.}

\item{formula}{One-sided formula in the covariates of \code{scores$data} (or
\code{data}), e.g. \code{~ x1 + I(x1^2)} or \code{~ 0 + group} for group effects.
\code{NULL} fits an intercept only, which returns the ATE.}

\item{data}{Optional data frame with \code{nrow(scores$data)} rows providing
extra regressors (for instance CATE predictions from another sample).}

\item{conf_level}{Confidence level.}

\item{uniform}{Compute the simultaneous critical value.}

\item{n_boot}{Multiplier bootstrap draws.}

\item{seed}{Optional seed for the bootstrap.}
}
\value{
A list of class \code{cm_blp}: \code{coefficients} (term, estimate,
std.error, statistic, p.value, conf.low, conf.high, band.low,
band.high), \code{vcov}, \code{crit_val} (simultaneous), \code{inffunc}, the design
information, and a Wald test that all non-intercept coefficients are
zero (\code{test_heterogeneity}). \code{\link[=predict]{predict()}} evaluates the curve on new
data with pointwise and simultaneous bands; \code{\link[=plot_cate_blp]{plot_cate_blp()}} draws it.
}
\description{
Regresses the doubly robust pseudo-outcome of \code{\link[=dr_scores]{dr_scores()}} on a
dictionary \code{p(X)} given by \code{formula}, yielding the best linear
approximation \verb{p(x)' beta_0} to the CATE (Semenova and Chernozhukov 2021;
Chernozhukov et al. 2026, ch. 14). Because the pseudo-outcome is
conditionally Neyman orthogonal, ordinary least squares with a
heteroskedasticity-robust (HC1) covariance is valid as if the nuisances
were known, provided the product of their errors vanishes faster than
\code{n^-1/2}. Simultaneous bands for the whole curve use the multiplier
bootstrap on the influence functions.
}
\examples{
dat <- sim_hte(1000, dgp = "smooth", seed = 1)
sc <- dr_scores(dat, "y", "d", paste0("x", 1:5), seed = 1)
blp <- cate_blp(sc, ~ x1 + I(x2^2), seed = 1)
blp
grid <- data.frame(x1 = seq(-2, 2, by = 0.5), x2 = 0)
predict(blp, grid)
}
\references{
Semenova, V. and Chernozhukov, V. (2021). Debiased machine learning of
conditional average treatment effects and other causal functions.
\emph{The Econometrics Journal}, 24(2), 264-289.
}
\seealso{
\code{\link[=cate_gate]{cate_gate()}}, \code{\link[=plot_cate_blp]{plot_cate_blp()}}
}
