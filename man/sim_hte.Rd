% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-hte.R
\name{sim_hte}
\alias{sim_hte}
\title{Simulate data with heterogeneous treatment effects}
\usage{
sim_hte(
  n = 1000L,
  dgp = c("smooth", "simple_cate", "complex_cate", "unbalanced", "binary_outcome",
    "policy"),
  p_treat = NULL,
  threshold = 0,
  sd_e = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{dgp}{Design:
\itemize{
\item \code{"smooth"}: five standard normal covariates, propensity
\verb{plogis(0.5 x1 - 0.5 x2)}, baseline \verb{x1 + x2^2 + 0.5 x3}, and a CATE
\code{1 + x1 + 0.5 (x2^2 - 1)} that depends on \code{x1} and \code{x2} only.
\item \code{"simple_cate"}: DGP 1 of Chernozhukov et al. (2026, ch. 15). One
covariate \code{x1 ~ U(0, 1)}, rare treatment (\code{p_treat = 0.05}), constant
effect 0.5, baseline \verb{0.3 * 1\{x1 in [0.6, 0.8]\}}, noise sd 0.05.
\item \code{"complex_cate"}: DGP 2. Effect \verb{0.5 * 1\{x1 in [0.6, 0.8]\}}, baseline
0.1, \code{p_treat = 0.05}.
\item \code{"unbalanced"}: DGP 3, the same as DGP 2 with \code{p_treat = 0.95}.
\item \code{"binary_outcome"}: the conversion example of Facure (2023, ch. 23).
A randomized nudge, \code{age ~ Gamma(10, 4)}, \verb{income ~ 100 Gamma(20, 2)},
latent index \verb{-4.5 + 0.001 income + nudge (1 + 0.01 age) + N(0, 1)},
outcome \verb{1\{latent > threshold\}}. Income moves the baseline only; age
moves the effect only.
\item \code{"policy"}: five uniform covariates on \verb{[-1, 1]}, a CATE that is
piecewise constant on a depth-two tree in \code{x1}, \code{x2}, \code{x3} plus a
small smooth term, confounded assignment \verb{plogis(0.5 x1 + 0.5 x4)}.
The optimal policy is the depth-two tree stored in
\code{attr(, "optimal_tree")}.
}}

\item{p_treat}{Treatment probability for the three Chernozhukov et al.
designs (ignored otherwise).}

\item{threshold}{Latent-index threshold for \code{"binary_outcome"} (0 gives
about 50 percent conversion, 2 about 12 percent, -2 about 93 percent).}

\item{sd_e}{Noise standard deviation (designs \code{"smooth"} and \code{"policy"}).}

\item{seed}{Optional seed.}
}
\value{
A data frame with columns \code{y}, \code{d}, the covariates, \code{tau_true},
\code{p_true}, \code{mu0_true}, \code{mu1_true}, and attribute \code{"dgp"}.
}
\description{
Designs used in the lecture on heterogeneous effects and policy learning.
Every design returns the true conditional average treatment effect
(\code{tau_true}), propensity score (\code{p_true}), and conditional means
(\code{mu0_true}, \code{mu1_true}) next to the observed data.
}
\examples{
dat <- sim_hte(500, dgp = "smooth", seed = 1)
head(dat)
mean(dat$tau_true)
}
\references{
Chernozhukov, V., Hansen, C., Kallus, N., Spindler, M., and Syrgkanis, V.
(2026). \emph{Applied Causal Inference Powered by ML and AI}, chapter 15.

Facure, M. (2023). \emph{Causal Inference for the Brave and True}, chapter 23.
}
