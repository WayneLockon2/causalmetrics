% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate-score.R
\name{cate_score}
\alias{cate_score}
\title{Compare CATE models out of sample with the doubly robust loss}
\usage{
cate_score(scores, ..., baseline = "constant", ate = NULL, conf_level = 0.95)
}
\arguments{
\item{scores}{A \code{cm_scores} object on the scoring sample.}

\item{...}{Named candidate models: \code{cm_cate} objects (predicted on
\code{scores$data}), numeric vectors of predictions, or column names of
\code{scores$data}. Avoid names that partially match \code{scores} (such as \code{s}
or \code{sc}); R would assign them to that argument.}

\item{baseline}{\code{"constant"} (default), which uses the constant model
\code{ate}, or the name of one of the candidates.}

\item{ate}{Constant effect used by the constant model; by default the mean
of the score on the scoring sample. Pass the training-sample ATE when
the scoring sample is small.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{cm_cate_score} with \code{table} (model, loss,
diff (baseline loss minus model loss; positive means better than the
baseline), std.error, conf.low, conf.high, score_norm), the
\code{predictions} matrix, the \code{baseline}, and \code{ate}.
}
\description{
For each candidate CATE model \code{tau_m}, computes the doubly robust loss
\code{L(tau_m) = mean((Y(eta) - tau_m(X))^2)} on the rows of \code{scores}, which
should be a held-out scoring sample. Differences of losses between two
models estimate differences of their mean squared errors against the true
CATE, are Neyman orthogonal to the nuisances, and are asymptotically normal
(Chernozhukov et al. 2026, Theorem 15.2.1), so each model is reported with
its loss difference from the baseline and a confidence interval. The
normalized score \code{1 - L(tau_m) / L(constant)} measures the improvement over
the constant-effect model.
}
\examples{
dat <- sim_hte(1500, dgp = "smooth", seed = 1)
x <- paste0("x", 1:5)
train <- dat[1:1000, ]; test <- dat[1001:1500, ]
m_dr <- cate_learner(train, "y", "d", x, x_het = c("x1", "x2"), method = "dr", seed = 1)
m_t <- cate_learner(train, "y", "d", x, x_het = c("x1", "x2"), method = "t", seed = 1)
sc_test <- dr_scores(test, "y", "d", x, seed = 2)
cate_score(sc_test, dr = m_dr, t = m_t)
}
\seealso{
\code{\link[=cate_ensemble]{cate_ensemble()}}, \code{\link[=cate_validate]{cate_validate()}}
}
