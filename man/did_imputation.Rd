% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did-imputation.R
\name{did_imputation}
\alias{did_imputation}
\title{Imputation estimator for staggered difference-in-differences}
\usage{
did_imputation(
  data,
  id,
  time,
  group,
  y,
  x = NULL,
  horizons = NULL,
  pre_window = 3L,
  cluster = NULL,
  weights = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{group}{Column name of the first treatment period (numeric;
never-treated units coded \code{0}, \code{NA}, or \code{Inf}).}

\item{y}{Column name of the outcome.}

\item{x}{Optional covariates (character vector or one-sided formula)
entered linearly in the first stage.}

\item{horizons}{Event times to report (default: all observed).}

\item{pre_window}{Number of pre-treatment leads for the placebo
regression on untreated observations (0 to skip).}

\item{cluster}{Optional cluster column for the standard errors (default:
unit).}

\item{weights}{Optional column name of sampling weights.}
}
\value{
An object of class \code{cm_did_imputation}: \code{by_event} (event time,
estimate, standard error, number of cells), \code{overall}, \code{by_group},
\code{pre} (lead coefficients from the untreated sample with clustered
standard errors), \code{cells} (imputed effects per treated observation),
the first-stage \code{fixest} model, and the call.
}
\description{
The imputation estimator of Borusyak, Jaravel, and Spiess (2024) (see also
Gardner 2022) fits unit and period effects on untreated observations only,
imputes the untreated potential outcome of every treated observation, and
averages the differences \code{Y - Y_hat(0)} by event time, cohort, or overall.
It uses every pre-treatment period as the baseline, which is efficient
when parallel trends holds in all periods and errors are homoskedastic
and serially uncorrelated, and it never compares treated units to
already-treated units.
}
\details{
Standard errors follow Borusyak, Jaravel, and Spiess (2024,
Section 4): the estimator is linear in the outcome, so its variance is
the sum over clusters of squared weighted residuals, with first-stage
residuals on untreated cells and, conservatively, deviations of the
imputed effects from their event-time mean on treated cells. The
imputation weights on untreated cells are computed exactly by solving
the sparse normal equations of the first stage.
}
\examples{
dat <- sim_did_panel(n_units = 300, n_periods = 8, groups = c(3, 6), seed = 1)
did_imputation(dat, id = "id", time = "time", group = "g", y = "y")
}
\references{
Borusyak, K., Jaravel, X., and Spiess, J. (2024). Revisiting event-study
designs: Robust and efficient estimation. \emph{Review of Economic Studies},
91(6), 3253-3285.

Gardner, J. (2022). Two-stage differences in differences. Working paper.
}
