% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did-permutation-test.R
\name{did_permutation_test}
\alias{did_permutation_test}
\title{Permutation (randomization) test for difference-in-differences}
\usage{
did_permutation_test(
  data,
  id,
  time,
  group,
  y,
  cluster = NULL,
  statistic = NULL,
  n_perm = 999L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{group}{Column with the first treatment period (0, \code{NA}, or \code{Inf}
for never treated), constant within cluster.}

\item{y}{Column name of the outcome.}

\item{cluster}{Column at whose level treatment is assigned and permuted
(default: the unit).}

\item{statistic}{Function of a data frame returning a scalar. The default
is the t-statistic of the static two-way fixed effects coefficient with
standard errors clustered by \code{cluster}.}

\item{n_perm}{Number of permutations.}

\item{seed}{Optional seed for the cross-fitting partition and the
bootstrap draws.}
}
\value{
An object of class \code{cm_did_permutation} with the observed
statistic, the two-sided permutation p-value, and the permutation
distribution.
}
\description{
With few treated clusters, cluster-robust standard errors and the
bootstrap are unreliable. A Fisher randomization test instead re-assigns
the treatment timing across clusters and recomputes a studentized
statistic; under random timing it is exact for the sharp null of no effect
for any unit, and with a studentized statistic it is asymptotically valid
for the weak null of a zero average effect (Roth and Sant'Anna 2023).
}
\examples{
dat <- sim_did_panel(n_units = 60, n_periods = 6, groups = 4, seed = 1)
did_permutation_test(dat, id = "id", time = "time", group = "g", y = "y", n_perm = 199)
}
\references{
Roth, J. and Sant'Anna, P. H. C. (2023). Efficient estimation
for staggered rollout designs. \emph{Journal of Political Economy:
Microeconomics}, 1(4), 669-709.
}
