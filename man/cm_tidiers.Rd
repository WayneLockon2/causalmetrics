% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidiers.R
\name{cm_tidiers}
\alias{cm_tidiers}
\alias{tidy.cm_aipw}
\alias{glance.cm_aipw}
\alias{tidy.cm_dml}
\alias{glance.cm_dml}
\title{Tidy and glance methods for causalmetrics estimators}
\usage{
\method{tidy}{cm_aipw}(x, conf.int = TRUE, conf.level = x$conf.level, ...)

\method{glance}{cm_aipw}(x, ...)

\method{tidy}{cm_dml}(x, conf.int = TRUE, conf.level = x$conf.level, ...)

\method{glance}{cm_dml}(x, ...)
}
\arguments{
\item{x}{A \code{cm_aipw} or \code{cm_dml} object.}

\item{conf.int}{Logical. Include confidence limits (default \code{TRUE}).}

\item{conf.level}{Confidence level; defaults to the level stored in the
object and is recomputed from the standard error otherwise.}

\item{...}{Unused.}
}
\value{
\code{tidy()}: a data frame with columns \code{term}, \code{estimate},
\code{std.error}, \code{statistic}, \code{p.value}, and (if requested) \code{conf.low},
\code{conf.high}. \code{glance()}: a one-row data frame with \code{nobs} and
method information.
}
\description{
\code{tidy()} returns the one-row coefficient table (the treatment effect) and
\code{glance()} returns one-row model information for \code{\link[=est_aipw]{est_aipw()}} and
\code{\link[=est_dml]{est_dml()}} objects. Both follow the \code{broom} conventions used by
\code{modelsummary}.
}
\examples{
set.seed(1)
n <- 500
x1 <- rnorm(n)
p <- plogis(0.5 * x1)
d <- rbinom(n, 1, p)
y <- 1 + x1 + 2 * d + rnorm(n)
dat <- data.frame(y = y, d = d, x1 = x1, p = p, mu0 = 1 + x1, mu1 = 3 + x1)
fit <- est_aipw(dat, "y", "d", p_hat = "p", mu0_hat = "mu0", mu1_hat = "mu1")
tidy(fit)
glance(fit)
}
