% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediate-iv.R
\name{mediate_iv}
\alias{mediate_iv}
\title{Mediation analysis with an instrumented mediator}
\usage{
mediate_iv(
  data,
  y,
  d,
  m,
  z,
  x = NULL,
  fe = NULL,
  cluster = NULL,
  weights = NULL,
  homogeneity_by = NULL,
  n_boot = 499L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d, m, z}{Outcome, treatment, mediator, and instrument column names
(\code{z} may be a vector of instruments).}

\item{x}{Optional control column names.}

\item{fe}{Optional fixed-effect column names.}

\item{cluster}{Optional cluster column for the bootstrap.}

\item{weights}{Optional column of regression weights (passed to every
\code{fixest} regression and carried through the bootstrap).}

\item{homogeneity_by}{Optional column defining subgroups for the
homogeneity check.}

\item{n_boot}{Bootstrap replications.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_med_iv} with \code{effects} (total from the reduced
form without \code{Z}, \code{total_z} with \code{Z}, direct, indirect, first_stage,
mediator_effect, and the naive OLS direct effect), \code{first_stage_F},
\code{homogeneity} (subgroup mediator effects), the fitted \code{fixest} models,
and settings.
}
\description{
Decomposes the total effect of a treatment \code{D} on \code{Y} into a direct effect
and an indirect effect through a mediator \code{M} that is endogenous, using an
instrument \code{Z} for the mediator. Three regressions, all with the same
controls and fixed effects (through \code{fixest}):
}
\details{
\itemize{
\item first stage: \code{M ~ D + Z + X}, giving the effect of \code{D} on \code{M}
(\code{delta_1}) and the instrument's strength;
\item second stage: \code{Y ~ D + M + X} with \code{M} instrumented by \code{Z}, giving the
direct effect \code{gamma_1} and the mediator effect \code{gamma_2};
\item reduced form: \code{Y ~ D + X} (the total effect) and \code{Y ~ D + Z + X}.
}

The indirect effect is \code{delta_1 * gamma_2}, and the direct plus indirect
effect equals the coefficient on \code{D} in the reduced form that includes
\code{Z} exactly. Beyond the usual instrument validity conditional on \code{D} and
\code{X}, the decomposition assumes a homogeneous mediator effect (the
compliers' \code{gamma_2} equals the population's; Huber 2019). The
\code{homogeneity_by} argument re-estimates \code{gamma_2} within subgroups as a
check. Standard errors come from a bootstrap of the whole system,
clustered when \code{cluster} is given.
}
\examples{
dat <- sim_mediation(2000, dgp = "iv_mediator", seed = 1)
fit <- mediate_iv(dat, "y", "d", "m", z = "z", x = c("x1", "x2"), n_boot = 99, seed = 1)
fit
attr(dat, "truth")[c("direct", "indirect")]
}
\references{
Cattan, S., Salvanes, K. G., and Tominey, E. (2025). First-generation
elite: the role of school social networks. \emph{American Economic Review},
115(12), 4369-4403.

Dippel, C., Ferrara, A., and Heblich, S. (2020). Causal mediation analysis
in instrumental-variables regressions. \emph{Stata Journal}, 20(3), 613-626.

Huber, M. (2019). A review of causal mediation analysis for assessing
direct and indirect treatment effects. IZA Discussion Paper 12660.
}
