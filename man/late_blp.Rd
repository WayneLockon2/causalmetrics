% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv-compliers.R
\name{late_blp}
\alias{late_blp}
\title{Best linear predictor of the complier CATE}
\usage{
late_blp(scores, formula, newdata, conf_level = 0.95)
}
\arguments{
\item{scores}{The list returned by \code{\link[=late_scores]{late_scores()}}.}

\item{formula}{One-sided formula in the covariates.}

\item{newdata}{Data frame on which to evaluate the ratio.}

\item{conf_level}{Confidence level.}
}
\value{
A data frame: \code{newdata} with \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, plus the numerator and denominator
predictions; the two \code{cm_blp} fits are attached as attributes.
}
\description{
Projects the two LATE score components of \code{\link[=late_scores]{late_scores()}} on a dictionary
\code{p(X)} and forms the ratio \verb{p(x)' b_outcome / p(x)' b_treatment} on
\code{newdata}, the conditional LATE \verb{E[Y(1) - Y(0) | X = x, complier]} when the
complier effect is linear in the dictionary (Chernozhukov et al. 2026,
eq. 12.3.1). Standard errors come from the joint influence functions of
the two regressions by the delta method.
}
