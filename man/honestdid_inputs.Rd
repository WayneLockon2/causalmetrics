% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/honestdid-inputs.R
\name{honestdid_inputs}
\alias{honestdid_inputs}
\title{Inputs for a HonestDiD sensitivity analysis}
\usage{
honestdid_inputs(x, ref = -1)
}
\arguments{
\item{x}{A dynamic \code{\link[=aggregate_att]{aggregate_att()}} object.}

\item{ref}{Reference event time to drop (default \code{-1}).}
}
\value{
A list with \code{betahat}, \code{sigma}, \code{numPrePeriods}, \code{numPostPeriods},
and \code{event_time}.
}
\description{
Rambachan and Roth (2023) sensitivity analysis needs the event-study
coefficients with the reference period removed and their covariance
matrix. \code{honestdid_inputs()} extracts both from a dynamic
\code{\link[=aggregate_att]{aggregate_att()}} object, with the covariance computed from the influence
functions (clustered when the underlying \code{att_gt()} was), so the result
can be passed directly to \code{HonestDiD::createSensitivityResults()} and
\code{HonestDiD::createSensitivityResults_relativeMagnitudes()}. Use
\code{base_period = "universal"} in \code{\link[=att_gt]{att_gt()}}, so the reference coefficient
at event time \code{-1} is exactly zero and every other pre-period is a
genuine pre-trend estimate.
}
\references{
Rambachan, A. and Roth, J. (2023). A more credible approach
to parallel trends. \emph{Review of Economic Studies}, 90(5), 2555-2591.
}
