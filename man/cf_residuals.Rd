% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-function.R
\name{cf_residuals}
\alias{cf_residuals}
\title{First-stage residuals for a control-function second stage}
\usage{
cf_residuals(
  data,
  d,
  z,
  x = NULL,
  family = c("gaussian", "probit", "logit"),
  name = "v_hat"
)
}
\arguments{
\item{data}{A data frame.}

\item{d}{Endogenous regressor column name.}

\item{z}{Character vector of instrument column names.}

\item{x}{Optional control column names.}

\item{family}{\code{"gaussian"} (OLS residual), \code{"probit"}, or \code{"logit"}
(generalized residual).}

\item{name}{Name of the control column.}
}
\value{
\code{data} with the control added; attribute \code{"first_stage"} holds the
fitted first stage and, for the Gaussian case, the first-stage F
statistics of \code{\link[=iv_first_stage]{iv_first_stage()}}.
}
\description{
Fits the first stage of an endogenous regressor \code{d} on the instruments
\code{z} and controls \code{x} and appends the control to the data: the OLS residual
for a continuous \code{d} (\code{family = "gaussian"}), or the generalized residual
of a probit or logit first stage for a binary \code{d}
(\verb{D lambda(xb) - (1 - D) lambda(-xb)} with \code{lambda} the inverse Mills
ratio for the probit; \code{D - Lambda(xb)} for the logit). Adding the control
to a linear second stage reproduces two-stage least squares exactly; adding
it to a probit, logit, Poisson, or discrete-choice second stage is the
control-function estimator of Rivers and Vuong (1988), Smith and Blundell
(1986), Wooldridge (2015), and Petrin and Train (2010).
}
\examples{
dat <- sim_iv(1000, dgp = "linear", seed = 1)
cf <- cf_residuals(dat, d = "d", z = "z", x = paste0("x", 1:5))
coef(lm(y ~ d + x1 + x2 + x3 + x4 + x5 + v_hat, data = cf))["d"]
}
\references{
Wooldridge, J. M. (2015). Control function methods in applied
econometrics. \emph{Journal of Human Resources}, 50(2), 420-445.
}
