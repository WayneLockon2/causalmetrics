% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr-scores.R
\name{bind_scores}
\alias{bind_scores}
\title{Pool several score objects}
\usage{
bind_scores(..., set = "set")
}
\arguments{
\item{...}{\code{cm_scores} objects, optionally named, or a single list of them.}

\item{set}{Name of the column added to \verb{$data} that records the origin.}
}
\value{
A \code{cm_scores} object whose \code{score}, \code{nuisance}, \code{residuals}, \code{y},
\code{d}, \code{data}, and \code{fold_id} are the stacked components (fold identifiers
are offset so they stay unique across sets), with the ATE and the
diagnostics recomputed on the pooled rows.
}
\description{
Stacks \code{cm_scores} objects built on different subsamples (for instance one
per treatment-control pair in a multi-treatment design) into one object,
so that \code{\link[=cate_blp]{cate_blp()}}, \code{\link[=cate_validate]{cate_validate()}}, or \code{\link[=policy_value]{policy_value()}} can be
applied to the pooled scores. A column named \code{set} in \verb{$data} records the
origin of each row (the names of the arguments, or \code{set1}, \code{set2}, ...).
}
\examples{
dat <- sim_hte(600, dgp = "smooth", seed = 1)
a <- dr_scores(dat[1:300, ], "y", "d", paste0("x", 1:5), seed = 1)
b <- dr_scores(dat[301:600, ], "y", "d", paste0("x", 1:5), seed = 2)
pooled <- bind_scores(first = a, second = b)
table(pooled$data$set)
}
