% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-checks.R
\name{rd_balance}
\alias{rd_balance}
\title{Covariate balance at the cutoff}
\usage{
rd_balance(
  data,
  covariates,
  x,
  cutoff = 0,
  h = NULL,
  p = 1,
  kernel = "triangular",
  cluster = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{covariates}{Character vector of covariate column names.}

\item{x}{Running variable column name.}

\item{cutoff}{Cutoff value.}

\item{h}{Common bandwidth for the joint test and, if given, for every
covariate; \code{NULL} lets \code{rdrobust} select a bandwidth per covariate and
uses their median for the joint test.}

\item{p}{Local polynomial order.}

\item{kernel}{Kernel.}

\item{cluster}{Optional cluster column name.}

\item{conf_level}{Confidence level.}
}
\value{
A list of class \code{cm_rd_balance} with \code{table} (covariate,
estimate, std.error, p.value, conf.low, conf.high, h, n_left,
n_right, all from the robust rows), \code{joint} (statistic, df, p.value,
the common bandwidth), and \code{cutoff}.
}
\description{
Runs the RD estimator with each covariate as the outcome, which tests
whether predetermined characteristics jump at the cutoff, and adds a
joint Wald test that all jumps are zero. Individual rows use
\code{rdrobust::rdrobust()} with robust bias-corrected inference; the joint
test uses the package's own local linear fit at one common bandwidth
with influence functions stacked across covariates, because \code{rdrobust}
does not expose the covariance across outcomes.
}
\examples{
dat <- sim_rd(2000, "covariates", seed = 1)
rd_balance(dat, paste0("z", 1:4), "x")
}
