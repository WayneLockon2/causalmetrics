% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/staggered-efficient.R
\name{staggered_efficient}
\alias{staggered_efficient}
\title{Efficient estimation for staggered rollouts with random timing}
\usage{
staggered_efficient(
  data,
  id,
  time,
  group,
  y,
  estimand = c("simple", "cohort", "calendar", "eventstudy"),
  event_time = 0,
  beta = NULL,
  control = c("notyet", "last"),
  n_perm = 0L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{group}{Column name of the first treatment period (numeric;
never-treated units coded \code{0}, \code{NA}, or \code{Inf}).}

\item{y}{Column name of the outcome.}

\item{estimand}{\code{"simple"} (average over all post-treatment cohort-period
cells, weighted by cohort size), \code{"cohort"}, \code{"calendar"}, or
\code{"eventstudy"} (effect \code{event_time} periods after adoption).}

\item{event_time}{Event time(s) for \code{estimand = "eventstudy"}.}

\item{beta}{\code{NULL} for the efficient plug-in \verb{beta*} (default), \code{1} for the
DiD (Callaway-Sant'Anna) weights, \code{0} for the difference in means, or
another scalar.}

\item{control}{\code{"notyet"} (all cohorts not yet treated at the comparison
period, default) or \code{"last"} (the last cohort only, as in Sun and
Abraham).}

\item{n_perm}{Number of permutations for the Fisher randomization test with
the studentized statistic (0 skips it).}

\item{seed}{Optional seed for the cross-fitting partition and the
bootstrap draws.}
}
\value{
An object of class \code{cm_staggered} with \code{estimates} (one row per
event time: \code{estimate}, \code{std.error} (the adjusted, less conservative
estimator), \code{se_neyman} (the conservative Neyman estimator), \code{beta},
\code{theta0}, \code{xhat}, and the permutation p-value when requested), the
cohort sizes, and the settings.
}
\description{
When treatment timing is (as good as) randomly assigned across units,
Roth and Sant'Anna (2023) show that difference-in-differences estimators
are unbiased but inefficient, and derive the efficient estimator in the
class \verb{theta_hat(beta) = theta_hat_0 - beta' X_hat}, where \code{theta_hat_0}
is the plug-in estimator that compares cohort means after treatment and
\code{X_hat} is the same comparison in the last pre-treatment period, which
has mean zero under no anticipation. \code{beta = 1} gives the Callaway-Sant'Anna
estimator with not-yet-treated comparisons, \code{beta = 0} the simple
difference in means, and the efficient \verb{beta*} is estimated from the
within-cohort covariance matrices of the outcome path. The function
reproduces the \code{staggered} package.
}
\details{
Under random timing the finite population of potential outcome
paths is fixed and only the assignment of cohorts is random. With
cohort means \code{Ybar_g} (a vector over periods) and within-cohort
covariance matrices \code{S_g}, any estimator in the class is linear in the
cohort means, \verb{theta_hat_0 = sum_g A_g Ybar_g} and
\verb{X_hat = sum_g A0_g Ybar_g}, so \code{Var(theta_hat_0, X_hat)} is estimable
from \code{S_g / N_g}, and \verb{beta* = Var(X_hat)^\{-1\} Cov(X_hat, theta_hat_0)}.
The Neyman variance ignores a non-estimable negative term (the variance
of individual effects) and is conservative; the adjusted variance
subtracts the part of that term that the pre-treatment outcomes
explain, following Section 4 of the paper. The permutation test
reassigns cohorts across units and recomputes the studentized
statistic; it is exact for the sharp null of no effect and
asymptotically valid for the weak null.
}
\examples{
dat <- sim_did_panel(n_units = 300, n_periods = 8, groups = c(3, 5, 7),
                     never_share = 0.3, x_select = 0, seed = 1)
staggered_efficient(dat, id = "id", time = "time", group = "g", y = "y", estimand = "simple")
staggered_efficient(dat, id = "id", time = "time", group = "g", y = "y",
                    estimand = "eventstudy", event_time = 0:2)
}
\references{
Roth, J. and Sant'Anna, P. H. C. (2023). Efficient estimation
for staggered rollout designs. \emph{Journal of Political Economy:
Microeconomics}, 1(4), 669-709.
}
