% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-iv.R
\name{sim_iv}
\alias{sim_iv}
\title{Simulate instrumental-variable designs}
\usage{
sim_iv(
  n = 1000L,
  dgp = c("linear", "weak", "late", "mte", "probit_cf", "shift_share", "judge"),
  pi = 1,
  concentration = 10,
  rho = 0.5,
  n_industries = 20L,
  n_judges = 30L,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{dgp}{Design:
\itemize{
\item \code{"linear"}: partially linear IV. Five standard normal covariates, an
unobserved confounder \code{a}, \verb{h(x) = 0.5 x1^2 + 0.5 sin(2 x2)}, instrument
\code{z = h(x) + e_z}, treatment \verb{d = pi z + 0.8 a + 0.5 x1^2 + e_d}, outcome
\verb{y = theta d + a + h(x) + 0.5 x3 + e_y}. The instrument is
excluded only conditional on nonlinear functions of \code{x1} and \code{x2}, so
linear controls leave bias. Attribute \code{theta}.
\item \code{"weak"}: three covariates entering linearly (\verb{h(x) = 0.5 x1 + 0.5 x2}),
the same confounder with small idiosyncratic noise (so the endogeneity
correlation is about 0.9), and a first-stage coefficient chosen so that
the concentration parameter is about \code{concentration}
(\code{pi = sqrt(concentration / n)}).
\item \code{"late"}: binary instrument assigned with probability \verb{plogis(0.5 x1)},
compliance types drawn with covariate-dependent probabilities
(always-takers, never-takers, compliers), complier effect
\code{2 + x1}, never-taker and always-taker effects that differ from it.
Attributes \code{late} (the population LATE) and \code{ate}.
\item \code{"mte"}: a Roy model with a continuous instrument. Unobserved
resistance \code{v ~ U(0, 1)}, propensity \verb{p(z, x) = plogis(-0.5 + z + 0.3 x1)},
\verb{d = 1\{p > v\}}, and a marginal treatment effect \code{mte(u) = 1.5 - 2 (u - 0.5) - 3 (u - 0.5)^2}
plus a covariate term \verb{0.5 x1}. Attributes \code{mte} (the function), \code{ate},
and \code{att}.
\item \code{"probit_cf"}: binary outcome with an endogenous continuous regressor.
\verb{d = 0.8 z + 0.5 x1 + v}, \verb{y* = -0.3 + 0.7 d + 0.5 x1 + u},
\verb{(u, v)} bivariate normal with correlation \code{rho}, \verb{y = 1\{y* > 0\}}.
Attribute \code{ape} (the true average partial effect of \code{d}).
\item \code{"shift_share"}: \code{n} regions with Dirichlet shares over \code{n_industries}
industries, national shocks \code{g_k}, regional treatment
\verb{d_i = sum_k s_ik (g_k + eta_ik) + u_i}, outcome
\verb{y_i = beta d_i + 0.5 x1 + e_i} with \code{u_i} correlated with \code{e_i}.
Shares are columns \verb{s1, ..., sK}, shocks are in attribute \code{shocks}.
\item \code{"judge"}: \code{n_judges} judges with leniencies drawn from \code{U(0.1, 0.9)},
random assignment, \verb{d = 1\{leniency + 0.3 x1 + 0.5 a > u\}} and
\verb{y = theta d + a + 0.5 x1 + e}. Attribute \code{theta}.
}}

\item{pi}{First-stage coefficient (designs \code{"linear"}, \code{"probit_cf"}).}

\item{concentration}{Concentration parameter for \code{"weak"}.}

\item{rho}{Error correlation (\code{"probit_cf"}).}

\item{n_industries, n_judges}{Sizes for the shift-share and judge designs.}

\item{seed}{Optional seed.}
}
\value{
A data frame with attribute \code{"dgp"} and the design's truth as
attributes.
}
\description{
Simulate instrumental-variable designs
}
\examples{
dat <- sim_iv(500, dgp = "late", seed = 1)
attr(dat, "late")
}
