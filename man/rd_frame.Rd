% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-frame.R
\name{rd_frame}
\alias{rd_frame}
\title{Tidy rows from regression discontinuity objects}
\usage{
rd_frame(..., methods = c("conventional", "bias_corrected", "robust"))
}
\arguments{
\item{...}{Named objects.}

\item{methods}{For \code{rdrobust} objects, which rows to keep (default all
three).}
}
\value{
A data frame with \code{model}, \code{term}, \code{method}, \code{estimate},
\code{std.error}, \code{conf.low}, \code{conf.high}, \code{p.value}, \code{h}, \code{n_eff}.
}
\description{
Collects estimates from \code{rdrobust::rdrobust()}, \code{RDHonest::RDHonest()},
\code{rdlocrand::rdrandinf()}, \code{rdmulti::rdmc()}, \code{rdhte::rdhte()}, and the
package's own \code{cm_rd_adjust} and \code{cm_rd_weak_iv} objects into one data
frame with common columns, so different inference procedures can be
compared in one table or figure.
}
\examples{
dat <- sim_rd(1500, "lee", seed = 1)
if (requireNamespace("rdrobust", quietly = TRUE)) {
  fit <- rdrobust::rdrobust(dat$y, dat$x, c = 0)
  rd_frame(rdrobust = fit)
}
}
