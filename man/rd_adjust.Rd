% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-adjust.R
\name{rd_adjust}
\alias{rd_adjust}
\title{Flexible covariate adjustment for regression discontinuity designs}
\usage{
rd_adjust(
  data,
  y,
  x,
  cutoff = 0,
  covariates,
  d = NULL,
  learner = NULL,
  h_train = NULL,
  weighted = FALSE,
  kernel = "triangular",
  pooled = TRUE,
  folds = 5L,
  seed = NULL,
  fit = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{y, x}{Outcome and running variable column names.}

\item{cutoff}{Cutoff value.}

\item{covariates}{Character vector of covariate column names.}

\item{d}{Optional treatment column name (fuzzy design).}

\item{learner}{\code{mlr3} regression learner (default linear regression;
\code{lrn("regr.ranger")}, \code{lrn("regr.cv_glmnet")}, ... for flexible
adjustment).}

\item{h_train}{Optional half-width of the window used to train the
learner (\code{NULL} uses all rows). Predictions are produced for all rows.}

\item{weighted}{Train with kernel weights \code{K((x - c) / h_train)}
(requires \code{h_train}).}

\item{kernel}{Kernel for the training weights.}

\item{pooled}{One model on both sides (\code{TRUE}) or the average of two
side-specific models (\code{FALSE}).}

\item{folds, seed}{Cross-fitting folds and seed.}

\item{fit}{Run \code{rdrobust} on the raw and adjusted outcomes.}

\item{...}{Arguments passed to \code{rdrobust::rdrobust()} (for instance
\code{p}, \code{kernel}, \code{bwselect}, \code{cluster}, \code{vce}, \code{h}).}
}
\value{
A list of class \code{cm_rd_adjust} with \code{data} (the input rows plus
\code{y_adj} and, for fuzzy designs, \code{d_adj}), \code{eta} (the predictions),
\code{fits} (raw and adjusted \code{rdrobust} objects when computed),
\code{comparison} (tidy rows of both fits), \code{r2} (share of outcome variance
explained by the adjustment on the estimation window), the learner
label and settings.
}
\description{
Subtracts a cross-fitted prediction of the outcome from the covariates,
\code{eta_hat(Z)}, before the local polynomial step. Because the RD estimand
is the jump at the cutoff of the conditional mean given \code{x}, and
\code{E[eta(Z) | x]} is continuous at the cutoff for any function \code{eta} of
the covariates alone, the jump of \code{Y - eta(Z)} equals the jump of \code{Y}.
The adjustment therefore needs no rate condition on the learner; it only
removes outcome variation explained by the covariates and shrinks the
variance (Noack, Olma, and Rothe 2024). With a linear learner it is the
local linear adjustment of Calonico et al. (2019) fitted globally; with
\code{regr.cv_glmnet} it is the local post-lasso of Kreiss and Rothe (2023).
}
\details{
The prediction is cross-fitted, so no observation's outcome enters the
model that adjusts it. Two variants: \code{pooled = TRUE} fits one model of
\code{Y} on \code{Z} on both sides of the cutoff; \code{pooled = FALSE} fits one model
per side and averages the two predictions, which is the authors'
recommended version when the covariate effects differ across sides
(both are functions of \code{Z} only). In fuzzy designs the treatment is
adjusted the same way.

When \code{rdrobust} is installed and \code{fit = TRUE}, the function also runs
\code{rdrobust::rdrobust()} on the raw and the adjusted outcome and reports
both, so the precision gain is visible in one call.
}
\examples{
dat <- sim_rd(2000, "covariates", seed = 1)
adj <- rd_adjust(dat, "y", "x", covariates = paste0("z", 1:4), seed = 1)
adj
}
\references{
Noack, C., Olma, T., and Rothe, C. (2024). Flexible covariate adjustments
in regression discontinuity designs. arXiv:2107.07942.

Calonico, S., Cattaneo, M. D., Farrell, M. H., and Titiunik, R. (2019).
Regression discontinuity designs using covariates. \emph{Review of Economics
and Statistics}, 101(3), 442-451.

Kreiss, A. and Rothe, C. (2023). Inference in regression discontinuity
designs with high-dimensional covariates. \emph{The Econometrics Journal},
26(2), 105-123.
}
