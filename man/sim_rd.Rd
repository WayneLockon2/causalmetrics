% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-rd.R
\name{sim_rd}
\alias{sim_rd}
\title{Simulate regression discontinuity designs}
\usage{
sim_rd(
  n = 1000L,
  dgp = c("lee", "fuzzy", "covariates", "cia", "kink", "discrete", "manipulated",
    "multi_cutoff"),
  tau = NULL,
  compliance = 0.8,
  sd_e = 0.1295,
  grid = 0.1,
  manip = 0.5,
  slope = 1,
  seed = NULL
)
}
\arguments{
\item{n}{Sample size.}

\item{dgp}{Design:
\itemize{
\item \code{"lee"}: sharp RD, \verb{d = 1\{x >= 0\}}, effect \code{tau}.
\item \code{"fuzzy"}: the cutoff moves the treatment probability from
\code{1 - compliance} to \code{compliance}; the effect of \code{d} on \code{y} is \code{tau}.
\item \code{"covariates"}: sharp RD with four covariates \code{z1}-\code{z4} that shift the
outcome (\code{z1}, \code{z2} strongly, nonlinearly in \code{z2}) and are smooth in
\code{x}; the covariate adjustment of \code{\link[=rd_adjust]{rd_adjust()}} removes their noise.
\item \code{"cia"}: sharp RD where the outcome depends on \code{x} only through a
covariate \code{z} correlated with \code{x}, so the conditional independence
assumption of Angrist and Rokkanen (2015) holds and effects away from
the cutoff equal \code{tau}.
\item \code{"kink"}: a sharp regression kink, \code{d = 1 + slope * max(x, 0)}, effect
\code{tau} per unit of \code{d} (the slope of \code{y} changes by \code{tau * slope}; the
level 1 keeps the treatment positive at the kink so an elasticity is
defined).
\item \code{"discrete"}: the \code{"lee"} design with \code{x} rounded to a grid of width
\code{grid}, as with grade point averages.
\item \code{"manipulated"}: the \code{"lee"} design in which a share \code{manip} of the
units just below the cutoff move just above it, with their untreated
outcomes raised, creating a density jump and a biased naive contrast.
\item \code{"multi_cutoff"}: three cutoffs at -0.5, 0, 0.5 (column \code{cutoff})
with effects \code{tau}, \code{tau + 0.03}, \code{tau + 0.06}.
}}

\item{tau}{Effect at the cutoff (default 0.04 in outcome units, 0.5 for
\code{"cia"}, 2 for \code{"kink"}).}

\item{compliance}{Treatment probability above the cutoff in \code{"fuzzy"}
(below it is \code{1 - compliance}).}

\item{sd_e}{Noise standard deviation (0.1295 in the CCT calibration).}

\item{grid}{Rounding width for \code{"discrete"}.}

\item{manip}{Share of units in \verb{(-0.1, 0)} moved above the cutoff in
\code{"manipulated"}.}

\item{slope}{Slope of the treatment above the kink in \code{"kink"}.}

\item{seed}{Optional seed.}
}
\value{
A data frame with \code{x}, \code{d}, \code{y}, design-specific columns, \code{mu0},
\code{mu1}, and attributes \code{"tau_true"}, \code{"dgp"}, \code{"cutoff"}.
}
\description{
All designs use the running variable \verb{x ~ 2 Beta(2, 4) - 1} with the
cutoff at 0 and the smooth conditional mean of the Lee (2008) design as
calibrated by Calonico, Cattaneo, and Titiunik (2014), whose jump at the
cutoff is 0.04. Each design returns the true effect at the cutoff in the
attribute \code{"tau_true"} and the two conditional means \code{mu0} and \code{mu1}.
}
\examples{
dat <- sim_rd(1000, "lee", seed = 1)
attr(dat, "tau_true")
}
\references{
Calonico, S., Cattaneo, M. D., and Titiunik, R. (2014). Robust
nonparametric confidence intervals for regression-discontinuity designs.
\emph{Econometrica}, 82(6), 2295-2326.
}
