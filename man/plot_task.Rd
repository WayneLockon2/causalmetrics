% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-task.R
\name{plot_task}
\alias{plot_task}
\title{Standardized plot template}
\usage{
plot_task(...)
}
\arguments{
\item{...}{Arguments passed to \code{ggplot()}. Typically \code{data} and \code{mapping}.
Add layers with \code{+} exactly as you would for \code{ggplot()}.}
}
\value{
A ggplot object with the standard causalmetrics styling applied.
}
\description{
Wraps \code{ggplot()} with the project's standard theme, color scales, and
legend position. Use \code{wrap_latex_figure()} after composing the plot to
produce a LaTeX figure block for inclusion in a paper.
}
\examples{
\dontrun{
library(ggplot2)
p <- plot_task(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point() +
  labs(x = "Weight", y = "MPG")

# For paper-ready LaTeX output:
tex <- wrap_latex_figure(
  p,
  caption = "Fuel efficiency vs. weight",
  notes   = "Each point is one car.",
  label   = "fig:mpg_weight",
  path    = "figures/mpg_weight.pdf"
)
}

}
