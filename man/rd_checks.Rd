% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-checks.R
\name{rd_checks}
\alias{rd_checks}
\title{The regression discontinuity diagnostic battery}
\usage{
rd_checks(
  data,
  y,
  x,
  cutoff = 0,
  covariates = NULL,
  d = NULL,
  cluster = NULL,
  h_grid = NULL,
  radius = NULL,
  cutoffs = NULL,
  conf_level = 0.95,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{Outcome column name.}

\item{x}{Running variable column name.}

\item{cutoff}{Cutoff value.}

\item{covariates}{Optional covariates for the balance table.}

\item{d}{Optional treatment column (fuzzy design).}

\item{cluster}{Optional cluster column name.}

\item{h_grid, radius, cutoffs}{Grids passed to the individual checks.}

\item{conf_level}{Confidence level.}

\item{...}{Passed to \code{rdrobust::rdrobust()}.}
}
\value{
A list of class \code{cm_rd_checks} with \code{main} (tidy rows of the
main \code{rdrobust} fit), \code{balance}, \code{placebo}, \code{sensitivity}, \code{donut}.
}
\description{
Runs the main estimate, covariate balance with a joint test, placebo
cutoffs, bandwidth sensitivity, and donut-hole estimates in one call and
returns them as one object with a print method and
\code{\link[=plot_rd_checks]{plot_rd_checks()}}.
}
\examples{
dat <- sim_rd(2000, "covariates", seed = 1)
ck <- rd_checks(dat, "y", "x", covariates = paste0("z", 1:4))
ck
plot_rd_checks(ck, "sensitivity")
}
