% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dml-sensitivity.R
\name{dml_sensitivity}
\alias{dml_sensitivity}
\title{Sensitivity of a DML estimate to unobserved confounding}
\usage{
dml_sensitivity(
  fit,
  r2_y = 0.05,
  r2_d = 0.05,
  conf_level = 0.95,
  grid = seq(0, 0.5, by = 0.01)
)
}
\arguments{
\item{fit}{A \code{cm_dml} object with \code{model = "plr"} or \code{"pliv"}.}

\item{r2_y, r2_d}{Partial R-squareds of the confounder with the outcome
and the treatment for the benchmark scenario.}

\item{conf_level}{Confidence level for the robustness value with
sampling uncertainty.}

\item{grid}{Values used for the contour grid.}
}
\value{
A list of class \code{cm_sensitivity} with \code{bias}, \code{bounds},
\code{robustness_value} (point and with the confidence interval), the
\code{scale} factor, and \code{contour} (data frame of \code{r2_d}, \code{r2_y}, \code{bias}).
}
\description{
In the partially linear model with an unobserved confounder \code{A}, the
bias of the partialling-out estimate is bounded by
\verb{|bias| <= sqrt(R2_y R2_d / (1 - R2_d)) * sqrt(E[(Y~ - theta D~)^2] / E[D~^2])},
where \code{R2_y} is the partial R-squared of \code{A} with the residualized outcome
(after the residualized treatment) and \code{R2_d} the partial R-squared of
\code{A} with the residualized treatment. The residual moments are available
from the \code{cm_dml} fit. The function returns the bound and the implied
interval for \code{theta} at user-supplied \verb{(R2_y, R2_d)}, the robustness value
(the common partial R-squared at which the bound reaches the estimate, or
the edge of its confidence interval), and a contour grid for plotting.
}
\examples{
set.seed(1)
n <- 800; x <- rnorm(n); d <- 0.5 * x + rnorm(n); y <- d + x^2 + rnorm(n)
fit <- est_dml(data.frame(y, d, x), "y", "d", "x", seed = 1)
dml_sensitivity(fit, r2_y = 0.05, r2_d = 0.02)
}
\references{
Chernozhukov, V., Cinelli, C., Newey, W., Sharma, A., and Syrgkanis, V.
(2022). Long story short: omitted variable bias in causal machine
learning. NBER Working Paper 30302.

Cinelli, C. and Hazlett, C. (2020). Making sense of sensitivity: extending
omitted variable bias. \emph{Journal of the Royal Statistical Society B},
82(1), 39-67.
}
