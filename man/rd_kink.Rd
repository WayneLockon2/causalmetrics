% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd-kink.R
\name{rd_kink}
\alias{rd_kink}
\title{Regression kink design}
\usage{
rd_kink(
  data,
  y,
  x,
  cutoff = 0,
  d = NULL,
  slope_change = 1,
  elasticity = FALSE,
  covariates = NULL,
  p = 1,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{y, x}{Outcome and running variable column names.}

\item{cutoff}{Kink point.}

\item{d}{Optional treatment column name (fuzzy kink); when \code{NULL} the
reduced-form slope change is reported and \code{slope_change} (a known
change in the treatment's slope, for sharp kinks) scales it.}

\item{slope_change}{Known change in the slope of the treatment at the
kink for sharp designs (default 1: the reduced form is reported).}

\item{elasticity}{Report the elasticity \code{kink * D(c) / Y(c)}.}

\item{covariates}{Optional covariates passed to \code{rdrobust}.}

\item{p}{Local polynomial order (default 1; Card et al. use local
linear and local quadratic).}

\item{...}{Passed to \code{rdrobust::rdrobust()} (\code{h}, \code{kernel}, \code{cluster},
\code{bwselect}, ...).}
}
\value{
A list of class \code{cm_rd_kink} with \code{reduced_form}, \code{first_stage},
\code{kink} (tidy rows of the ratio or the scaled reduced form),
\code{elasticity}, \code{levels} (outcome and treatment at the kink), and the
\code{rdrobust} objects in \code{fits}.
}
\description{
Estimates the change in the slope of the outcome at the kink (the reduced
form) and, when a treatment variable is given, the change in the slope of
the treatment (the first stage) and their ratio (the fuzzy kink
estimator of Card, Lee, Pei, and Weber 2015), all through
\code{rdrobust::rdrobust(deriv = 1)}. The elasticity of the outcome with
respect to the treatment is the ratio scaled by the levels of the
treatment and the outcome at the kink.
}
\examples{
dat <- sim_rd(3000, "kink", seed = 1)
rd_kink(dat, "y", "x", d = "d")
}
\references{
Card, D., Lee, D. S., Pei, Z., and Weber, A. (2015). Inference on causal
effects in a generalized regression kink design. \emph{Econometrica}, 83(6),
2453-2483.
}
