% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediate-reg.R
\name{mediate_reg}
\alias{mediate_reg}
\title{Regression-based causal mediation analysis}
\usage{
mediate_reg(
  data,
  y,
  d,
  m,
  x = NULL,
  interaction = FALSE,
  outcome = c("linear", "logit"),
  mediator = c("linear", "logit"),
  method = c("simulation", "bootstrap", "delta"),
  m_ref = NULL,
  cluster = NULL,
  weights = NULL,
  n_boot = 499L,
  n_sim = 1000L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d}{Outcome and binary treatment column names.}

\item{m}{Character vector of mediator names (one mediator, or several
parallel mediators that do not cause each other).}

\item{x}{Optional covariate names.}

\item{interaction}{Include treatment-mediator interactions in the outcome
model.}

\item{outcome, mediator}{\code{"linear"} or \code{"logit"} model for the outcome and
for the mediators (recycled across mediators).}

\item{method}{Inference: \code{"simulation"} (quasi-Bayesian draws of the
coefficients from their robust covariance, Imai et al. 2010),
\code{"bootstrap"} (nonparametric, clustered when \code{cluster} is given), or
\code{"delta"} (Sobel's formula for a single mediator with linear models and
no interaction, treating the two regressions as independent).}

\item{m_ref}{Mediator value(s) at which the controlled direct effect is
evaluated (default 0 for each mediator).}

\item{cluster}{Optional cluster column for the covariance or the bootstrap.}

\item{weights}{Optional column of regression weights (used in every
model and in the averaging over units).}

\item{n_boot}{Bootstrap replications.}

\item{n_sim}{Coefficient draws for \code{method = "simulation"}, and Monte Carlo
draws of a continuous mediator inside a nonlinear outcome model.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_mediation} with \code{effects} (term, estimate,
std.error, conf.low, conf.high for total, nde, nie, nde_total, nie_pure,
cde, and per-mediator \verb{nie_<m>} when several mediators are given),
\code{shares} (proportion mediated with a standard error from the draws or
the bootstrap; with linear models also the two shares
of Wheeler et al. 2022, \code{S1 = alpha * gamma / beta} with \code{alpha} the
mediator coefficient in the outcome model, \code{gamma} the treatment effect
on the mediator, \code{beta} the total effect, and \code{S2 = delta * gamma / beta}
with \code{delta} the mediator-outcome association in the control group,
whose standard errors come from the delta method on the stacked
influence functions of the regressions, so cross-equation covariances
are carried, as in Stata's \code{suest}),
\code{potential} (the four \verb{E[Y(d, M(d'))]}), \code{models}, and the settings.
}
\description{
Estimates the total effect, the natural direct effect (NDE), the natural
indirect effect (NIE), and the controlled direct effect (CDE) of a binary
treatment through one or several parallel mediators from a mediator model
\code{M ~ D + X} and an outcome model \code{Y ~ D + M + X} (optionally with \code{D:M}),
under sequential ignorability (Imai, Keele, and Yamamoto 2010): no
unmeasured treatment-outcome, treatment-mediator, or mediator-outcome
confounding given \code{X}, and no mediator-outcome confounder affected by the
treatment.
}
\details{
Effects are computed by g-computation over the sample: for each unit the
mediator model gives the distribution of \verb{M(d')} and the outcome model the
mean of \verb{Y(d, M(d'))}; averaging over units gives \verb{E[Y(d, M(d'))]} for the
four combinations of \verb{d, d'}. With linear models and no interaction this
reduces to the product of coefficients (Baron and Kenny 1986; Sobel 1982),
and with the interaction to the closed forms of VanderWeele (2015). Binary
mediators or outcomes use logistic models and the effects are on the
probability scale.
}
\examples{
dat <- sim_mediation(1000, dgp = "linear", seed = 1)
fit <- mediate_reg(dat, "y", "d", "m", x = c("x1", "x2"), seed = 1)
fit
attr(dat, "truth")[c("total", "nde", "nie")]
}
\references{
Imai, K., Keele, L., and Yamamoto, T. (2010). Identification, inference and
sensitivity analysis for causal mediation effects. \emph{Statistical Science},
25(1), 51-71.

VanderWeele, T. J. (2015). \emph{Explanation in Causal Inference}. Oxford.

Wheeler, L., Garlick, R., Johnson, E., Shaw, P., and Gargano, M. (2022).
LinkedIn(to) job opportunities. \emph{AEJ: Applied Economics}, 14(2), 101-125.
}
\seealso{
\code{\link[=mediate_dml]{mediate_dml()}}, \code{\link[=mediate_sensitivity]{mediate_sensitivity()}}, \code{\link[=plot_mediation]{plot_mediation()}}
}
