% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/att-gt.R
\name{att_gt}
\alias{att_gt}
\title{Group-time average treatment effects (Callaway and Sant'Anna 2021)}
\usage{
att_gt(
  data,
  id = NULL,
  time,
  group,
  y,
  x = NULL,
  method = c("dr", "reg", "ipw"),
  control_group = c("notyet", "never"),
  base_period = c("varying", "universal"),
  anticipation = 0L,
  sampling = c("panel", "rcs"),
  weights = NULL,
  cluster = NULL,
  learner_p = NULL,
  learner_or = NULL,
  folds = 5L,
  seed = NULL,
  n_boot = 999L,
  boot_weights = c("mammen", "rademacher"),
  conf_level = 0.95,
  p_trim = 0.995,
  keep_inffunc = TRUE
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{group}{Column name of the first treatment period (numeric;
never-treated units coded \code{0}, \code{NA}, or \code{Inf}).}

\item{y}{Column name of the outcome.}

\item{x}{Covariates for conditional parallel trends: \code{NULL}, a character
vector of column names, or a one-sided formula. Covariates are taken
from the earlier of the two periods in each cell.}

\item{method}{\code{"dr"} (doubly robust, default), \code{"reg"} (outcome
regression), or \code{"ipw"} (Hajek-normalized inverse probability weighting).}

\item{control_group}{\code{"notyet"} (units not yet treated by the later of the
current and base period, default) or \code{"never"}.}

\item{base_period}{\code{"varying"} (default; the period just before \code{t} for
pre-treatment cells and \code{g - 1 - anticipation} for post-treatment cells)
or \code{"universal"} (always \code{g - 1 - anticipation}, so the base period
itself has \code{ATT = 0} by construction).}

\item{anticipation}{Number of periods before \code{g} in which units may already
respond; shifts the base period back.}

\item{sampling}{\code{"panel"} (balanced panel, default) or \code{"rcs"} (repeated
cross-sections; \code{id} may then be omitted).}

\item{weights}{Optional column name of sampling weights.}

\item{cluster}{Optional column name for clustered inference (must be
constant within unit).}

\item{learner_p, learner_or}{Optional \code{mlr3} learners for the propensity
score and the outcome regression \code{E[Y_t - Y_base | X, comparison]}.
Supplying either switches the cells to cross-fitted machine-learning
nuisances (panel data only).}

\item{folds}{Cross-fitting folds for the learner path.}

\item{seed}{Optional seed for the cross-fitting partition and the
bootstrap draws.}

\item{n_boot}{Number of multiplier-bootstrap draws (default 999). \code{0}
returns analytic standard errors and pointwise intervals only.}

\item{boot_weights}{\code{"mammen"} (default) or \code{"rademacher"} multipliers.}

\item{conf_level}{Confidence level for intervals and the uniform band.}

\item{p_trim}{Comparison units with a propensity score above this value
are dropped from the cell (as in \code{DRDID}).}

\item{keep_inffunc}{Keep the sparse influence-function matrix (needed by
\code{\link[=aggregate_att]{aggregate_att()}}).}
}
\value{
An object of class \code{cm_att_gt}: \code{att_gt} (one row per cell with
\code{group}, \code{time}, \code{event_time}, \code{post}, \code{att}, \code{std.error}, \code{conf.low},
\code{conf.high}, \code{band.low}, \code{band.high}, \code{n_treated}, \code{n_control}),
\code{crit_val} (uniform band critical value), \code{pretest} (Wald test that all
pre-treatment cells are zero), \code{inffunc} (sparse \verb{n x K} influence
functions), \code{units} (unit-level group, weight, cluster), and the settings.
}
\description{
\code{att_gt()} estimates the building block of modern difference-in-differences
with staggered adoption: the average treatment effect at calendar time \code{t}
among units first treated in period \code{g}, \code{ATT(g, t)}, for every pair
\verb{(g, t)}. Each cell is a two-by-two comparison of the long difference
\verb{Y_t - Y_\{base\}} between group \code{g} and a comparison group (units never
treated, or units not yet treated by \code{t}). With covariates the cell is
estimated by outcome regression, inverse probability weighting, or the
doubly robust score of Sant'Anna and Zhao (2020). A two-group design is
the special case of one cohort, so the function also covers the canonical
two-by-two and two-group event studies.
}
\details{
The function owns the causal layer only: cell definitions, the scores,
influence functions, the multiplier bootstrap, and simultaneous bands.
Regressions and logits inside the cells are parametric (as in \code{DRDID})
unless \code{mlr3} learners are supplied, in which case the nuisances are
cross-fitted within each cell and the plain orthogonal score is used.

\strong{Cells.} For group \code{g} and period \code{t}, the treated sample is group \code{g}
and the comparison sample is the never-treated units or, with
\code{control_group = "notyet"}, all units whose first treatment period exceeds
\code{max(t, base) + anticipation}. The outcome is the long difference
\code{Y_t - Y_base}. Cells with \code{t >= g} are post-treatment (\code{post = 1}); the
others are pre-treatment placebos.

\strong{Influence functions.} Every cell returns a unit-level influence
function, zero for units outside the cell and scaled by \code{n / n_cell}.
Standard errors are \verb{sqrt(sum_c (sum_\{i in c\} IF_i)^2) / n} analytically,
or the interquartile-range estimate from the multiplier bootstrap. The
uniform band uses the bootstrap distribution of the maximum studentized
deviation across cells.
}
\examples{
dat <- sim_did_panel(n_units = 300, n_periods = 8, seed = 1)
fit <- att_gt(dat, id = "id", time = "time", group = "g", y = "y",
              x = c("x1", "x2"), control_group = "notyet", n_boot = 99)
fit
aggregate_att(fit, type = "dynamic")
}
\references{
Callaway, B. and Sant'Anna, P. H. C. (2021). Difference-in-differences
with multiple time periods. \emph{Journal of Econometrics}, 225(2), 200-230.

Sant'Anna, P. H. C. and Zhao, J. (2020). Doubly robust
difference-in-differences estimators. \emph{Journal of Econometrics}, 219(1),
101-122.
}
