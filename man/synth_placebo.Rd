% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-inference.R
\name{synth_placebo}
\alias{synth_placebo}
\title{Placebo tests for a synthetic control}
\usage{
synth_placebo(
  x,
  type = c("space", "time"),
  placebo_time = NULL,
  mspe_limit = Inf,
  include_treated = FALSE,
  lags = "all",
  pre_window = NULL
)
}
\arguments{
\item{x}{A \code{cm_synth} object in the average mode.}

\item{type}{\code{"space"} (default) or \code{"time"}.}

\item{placebo_time}{For \code{type = "time"}, the pseudo adoption period; must
be a pre-treatment period with at least two periods before it.}

\item{mspe_limit}{For \code{type = "space"}, placebos whose pre-treatment MSPE
exceeds \code{mspe_limit} times the treated unit's are dropped from the
p-values (Andersson 2019 uses 20). The full set is kept in the object.}

\item{include_treated}{For \code{type = "space"}, keep the real treated unit in
the donor pool of each placebo (default \code{FALSE}, as in Abadie et al.).}

\item{lags, pre_window}{For \code{type = "time"}, the predictor lags and the
covariate window relative to the placebo date; default all pre-placebo
periods.}
}
\value{
For \code{type = "space"}, an object of class \code{cm_synth_placebo} with
\code{gaps} (one column per unit, the treated unit first), \code{mspe} (pre and
post MSPE, their ratio, and the rank of each unit), \code{p_value} (share of
kept units, the treated one included, whose ratio is at least the
treated unit's), \code{p_value_by_period} (share of kept units whose absolute
gap in each post period is at least the treated unit's), \code{kept}, and
the treated fit. For \code{type = "time"}, a \code{cm_synth} object fitted at the
placebo date.
}
\description{
In-space placebos reassign the treatment to each donor and re-estimate
the synthetic control with the same predictors, giving the permutation
distribution of the gap and of the post/pre mean squared prediction error
(MSPE) ratio (Abadie, Diamond, and Hainmueller 2010). In-time placebos
move the treatment date to an earlier period, using only the data before
the real treatment (backdating; Abadie, Diamond, and Hainmueller 2015).
}
\examples{
dat <- sim_synth_panel(n_donors = 12, t_pre = 12, t_post = 4, effect = 3, seed = 2)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d")
pl <- synth_placebo(fit)
pl$p_value
}
\references{
Abadie, A., Diamond, A., and Hainmueller, J. (2015).
Comparative politics and the synthetic control method. \emph{American
Journal of Political Science}, 59(2), 495-510.
}
