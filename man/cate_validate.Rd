% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate-validate.R
\name{cate_validate}
\alias{cate_validate}
\title{Validate a CATE model on a held-out sample}
\usage{
cate_validate(
  scores,
  tau_hat,
  n_groups = 4L,
  quantile_grid = seq(0.05, 1, by = 0.05),
  breaks = NULL,
  covariates = NULL,
  n_boot = 999L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{scores}{A \code{cm_scores} object on the test sample.}

\item{tau_hat}{A \code{cm_cate} object (predicted on \code{scores$data}), a numeric
vector, or a column name.}

\item{n_groups}{Number of quantile groups for calibration.}

\item{quantile_grid}{Shares \code{q} at which the curves are evaluated.}

\item{breaks}{Optional quantile thresholds of \code{tau_hat} computed on
non-test data (a list with \code{groups} and \code{curve} elements, or \code{NULL} to
use the test-sample quantiles).}

\item{covariates}{Covariates for the group-difference table (default the
\code{x} of \code{scores}).}

\item{n_boot}{Multiplier bootstrap draws.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_cate_val} with \code{blp} (the heterogeneity
regression table), \code{calibration} (group table, \code{cal1}, \code{cal2}, and
\code{test_equal}), \code{curves} (data frame with \code{q}, \code{toc}, \code{qini}, standard
errors, pointwise and one-sided simultaneous lower bounds), \code{areas}
(\code{autoc}, \code{auqc} with standard errors and lower bounds), \code{group_diff},
and \code{tau_hat}. \code{\link[=plot_cate_validation]{plot_cate_validation()}} draws the pieces.
}
\description{
Runs the diagnostics of Chernozhukov et al. (2026, Section 15.3) for a
fixed CATE model \code{tau_hat} using the doubly robust pseudo-outcomes of a
test sample:
}
\details{
\itemize{
\item \strong{Heterogeneity test.} OLS of the pseudo-outcome on
\verb{(1, tau_hat - mean(tau_hat))}. The slope estimates
\code{Cov(Y(1) - Y(0), tau_hat(X)) / Var(tau_hat(X))}; a significant slope
means the model is correlated with the true effect, and the ideal value
is 1. The intercept is the ATE.
\item \strong{Calibration.} Groups by quantiles of \code{tau_hat}; in each group the
doubly robust group average effect is compared with the mean prediction.
\code{cal1} and \code{cal2} are the group-weighted absolute and squared
discrepancies; the Wald test checks whether the groups differ.
\item \strong{Targeting curves.} For each share \code{q}, \code{TOC(q)} is the average effect
among the top-\code{q} predicted units minus the ATE and \verb{QINI(q) = TOC(q) q}
(both are covariances between the individual effect and the targeting
indicator). Pointwise standard errors come from the influence functions,
simultaneous one-sided bands from the multiplier bootstrap. The areas
\code{AUTOC} and \code{AUQC} integrate the curves over all ranks (the rank-weighted
average treatment effects of Yadlowsky et al. 2021, as in
\code{grf::rank_average_treatment_effect()}) and are reported with one-sided
intervals from their influence functions. Ties in
\code{tau_hat} are broken at random with the rule of their Remark 15.3.1.
\item \strong{Group differences.} Covariate means in the top and bottom groups.
}
}
\examples{
dat <- sim_hte(2000, dgp = "smooth", seed = 1)
x <- paste0("x", 1:5)
train <- dat[1:1200, ]; test <- dat[1201:2000, ]
m <- cate_learner(train, "y", "d", x, x_het = c("x1", "x2"), method = "dr", seed = 1)
sc_test <- dr_scores(test, "y", "d", x, seed = 2)
v <- cate_validate(sc_test, m, n_boot = 199, seed = 1)
v
}
\references{
Yadlowsky, S., Fleming, S., Shah, N., Brunskill, E., and Wager, S. (2021).
Evaluating treatment prioritization rules via rank-weighted average
treatment effects. arXiv:2111.07966.
}
\seealso{
\code{\link[=cate_score]{cate_score()}}, \code{\link[=plot_cate_validation]{plot_cate_validation()}}
}
