% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate-score.R
\name{cate_ensemble}
\alias{cate_ensemble}
\title{Stack CATE models on a scoring sample}
\usage{
cate_ensemble(
  scores,
  ...,
  method = c("q", "convex", "best", "ols"),
  ate = NULL,
  max_iter = 5000L,
  tol = 1e-10
)
}
\arguments{
\item{scores}{A \code{cm_scores} object on the scoring sample.}

\item{...}{Named \code{cm_cate} objects (they must be able to predict on new
data).}

\item{method}{\code{"q"}, \code{"convex"}, \code{"best"}, or \code{"ols"}.}

\item{ate}{Intercept of the stacked model (default the mean score on the
scoring sample; the training-sample ATE is preferable when the scoring
sample is small).}

\item{max_iter, tol}{Frank-Wolfe settings for the simplex problems.}
}
\value{
A \code{cm_cate} object with \code{method = "ensemble"}, whose \code{ensemble}
element records the rule, weights, base models, centering means, and
losses. \code{\link[=predict]{predict()}} combines the base models on new data.
}
\description{
Combines several fitted CATE models into one by minimizing the doubly
robust loss on the scoring sample. \code{"best"} picks the single model with the
smallest loss; \code{"convex"} finds weights on the simplex; \code{"q"}
(Q-aggregation, Lecué and Rigollet 2014; Lan and Syrgkanis 2023) adds the
penalty \verb{sum_m w_m L(tau_m)}, which yields the optimal \code{log(M) / n} rate;
\code{"ols"} is unconstrained least squares. Base predictions are centered
and the intercept is fixed at \code{ate} (Chernozhukov et al. 2026,
Remark 15.2.3).
}
\examples{
dat <- sim_hte(1500, dgp = "smooth", seed = 1)
x <- paste0("x", 1:5)
train <- dat[1:1000, ]; test <- dat[1001:1500, ]
m_dr <- cate_learner(train, "y", "d", x, x_het = c("x1", "x2"), method = "dr", seed = 1)
m_r <- cate_learner(train, "y", "d", x, x_het = c("x1", "x2"), method = "r", seed = 1)
sc_test <- dr_scores(test, "y", "d", x, seed = 2)
ens <- cate_ensemble(sc_test, dr = m_dr, r = m_r, method = "q")
ens
}
\references{
Lecué, G. and Rigollet, P. (2014). Optimal learning with Q-aggregation.
\emph{Annals of Statistics}, 42(1), 211-224.

Lan, H. and Syrgkanis, V. (2023). Causal Q-aggregation for CATE model
selection. arXiv:2310.16945.
}
