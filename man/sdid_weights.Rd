% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdid.R
\name{sdid_weights}
\alias{sdid_weights}
\title{Synthetic difference-in-differences weights}
\usage{
sdid_weights(
  data,
  id,
  time,
  y,
  d,
  estimator = c("sdid", "sc", "did", "difp"),
  zeta_omega = NULL,
  zeta_lambda = NULL,
  sparsify = TRUE,
  max_iter = 10000L,
  min_decrease = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{y}{Column name of the outcome.}

\item{d}{Column name of the treatment indicator (0/1; 1 for treated units
in post-treatment periods).}

\item{estimator}{\code{"sdid"} (default), \code{"sc"}, \code{"did"}, or \code{"difp"}.}

\item{zeta_omega, zeta_lambda}{Regularization for the unit and time
weights. Defaults follow Arkhangelsky et al. (2021):
\code{zeta_omega = (N1 * T1)^(1/4) * sigma} and \code{zeta_lambda = 1e-6 * sigma}
for \code{"sdid"}, where \code{sigma} is the standard deviation of first
differences of untreated outcomes before treatment; \code{"sc"} and \code{"difp"}
use \code{1e-6 * sigma} for the unit weights.}

\item{sparsify}{Zero out unit and time weights below a quarter of the
largest weight and re-optimize, as \code{synthdid} does.}

\item{max_iter, min_decrease}{Frank-Wolfe iteration limit and stopping rule.}
}
\value{
An object of class \code{cm_sdid} with \code{estimate}, \code{omega} (unit
weights of the untreated units), \code{lambda} (time weights of the
pre-treatment periods), \code{cell_weights} (a data frame with \code{id}, \code{time},
and \code{weight} for the weighted regression), \code{sigma}, \code{zeta}, the
dimensions \code{N0}, \code{N1}, \code{T0}, \code{T1}, the outcome matrix, and the call.
}
\description{
\code{sdid_weights()} computes the unit and time weights of synthetic
difference-in-differences (Arkhangelsky et al. 2021) for a block design in
which a set of units adopts treatment in the same period. It returns the
weights, the point estimate, and a cell-weight column so that the
difference-in-differences step can be run as a weighted two-way fixed
effects regression with \code{fixest::feols(y ~ d | id + time, weights = w)}.
The same function produces the weights of the synthetic control estimator
(\code{"sc"}), the plain difference-in-differences (\code{"did"}), and the
difference-in-differences with unit weights only (\code{"difp"}), so the four
estimators can be compared on one footing.
}
\details{
Unit weights solve
\verb{min ||omega_0 + Y_pre' omega - Ybar_treated_pre||^2 + zeta^2 T0 ||omega||^2}
over the simplex, and time weights solve the analogous problem that
matches pre-treatment to post-treatment outcomes of the untreated units.
The estimate is the weighted double difference, which equals the
coefficient on \code{d} in the two-way fixed effects regression weighted by
\code{omega_i * lambda_t} (treated cells weight one).
}
\examples{
dat <- sim_did_panel(n_units = 40, n_periods = 12, groups = 9, never_share = 0.8, seed = 1)
w <- sdid_weights(dat, id = "id", time = "time", y = "y", d = "treated")
w
fit <- fixest::feols(y ~ treated | id + time, data = merge(dat, w$cell_weights), weights = ~weight)
coef(fit)
}
\references{
Arkhangelsky, D., Athey, S., Hirshberg, D. A., Imbens, G. W.,
and Wager, S. (2021). Synthetic difference-in-differences. \emph{American
Economic Review}, 111(12), 4088-4118.
}
