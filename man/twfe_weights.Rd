% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twfe-diagnostics.R
\name{twfe_weights}
\alias{twfe_weights}
\title{Weights that static two-way fixed effects puts on each treated cell}
\usage{
twfe_weights(data, id, time, d, y = NULL)
}
\arguments{
\item{data}{A balanced panel in long format.}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period.}

\item{d}{Column name of the treatment indicator (0/1, absorbing: once 1,
stays 1). Units treated in the first period are always treated and are
dropped with a warning, as in the original decomposition.}

\item{y}{Optional outcome; when given, the function verifies the identity
\verb{beta = sum(eps * y) / sum_\{treated\} eps} that underlies the weights.}
}
\value{
An object of class \code{cm_twfe_weights} with \code{cells} (unit-period
weights on treated cells), \code{by_cell} (weights summed by first-treatment
period and calendar period), \code{share_negative}, \code{sum_negative}, and, when
\code{y} is supplied, \code{twfe} and \code{check}.
}
\description{
de Chaisemartin and D'Haultfoeuille (2020) show that the static TWFE
coefficient equals a weighted sum of the treatment effects of the treated
unit-period cells, with weights that sum to one but can be negative. The
weight of cell \verb{(i, t)} is the residual of \code{d} on unit and time effects
divided by the sum of those residuals over treated cells. \code{twfe_weights()}
computes them, summarizes them by cohort and period, and reports the share
and mass of negative weights.
}
\examples{
dat <- sim_did_panel(n_units = 200, n_periods = 8, groups = c(3, 6), seed = 1)
twfe_weights(dat, id = "id", time = "time", d = "treated", y = "y")
}
\references{
de Chaisemartin, C. and D'Haultfoeuille, X. (2020). Two-way
fixed effects estimators with heterogeneous treatment effects.
\emph{American Economic Review}, 110(9), 2964-2996.
}
