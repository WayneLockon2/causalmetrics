% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/att-dose.R
\name{att_dose}
\alias{att_dose}
\title{Difference-in-differences with a continuous or multi-valued treatment}
\usage{
att_dose(
  data,
  id,
  time,
  y,
  dose,
  dose_type = c("binned", "spline"),
  n_bins = 4L,
  breaks = NULL,
  df = 4L,
  grid = NULL,
  n_boot = 199L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame or \code{data.table} in long format (one row per unit
and period for panel data; one row per observation for repeated
cross-sections).}

\item{id}{Column name of the unit identifier.}

\item{time}{Column name of the period (numeric).}

\item{y}{Column name of the outcome.}

\item{dose}{Column with the dose received in the post-treatment period
(constant within unit; 0 for untreated units).}

\item{dose_type}{\code{"binned"} (default; doses grouped by \code{breaks} or into
\code{n_bins} quantile bins) or \code{"spline"} (a B-spline regression of the
outcome change on the dose among treated units).}

\item{n_bins, breaks}{Bins for \code{dose_type = "binned"}.}

\item{df}{Degrees of freedom of the B-spline for \code{dose_type = "spline"}.}

\item{grid}{Dose values at which the spline curve is evaluated (default:
50 points over the treated dose range).}

\item{n_boot}{Bootstrap replications for the spline curve (units resampled
with replacement).}

\item{conf_level}{Confidence level for intervals and the uniform band.}

\item{seed}{Optional seed for the cross-fitting partition and the
bootstrap draws.}
}
\value{
An object of class \code{cm_att_dose} with \code{by_dose} (level effects by
bin with standard errors), \code{acr} (causal responses between consecutive
bins, valid under strong parallel trends), \code{curve} (spline level effects
and derivatives on the grid with bootstrap bands, when
\code{dose_type = "spline"}), \code{twfe} (the regression coefficient of the
outcome change on the dose), and \code{twfe_weights} (the two decompositions).
}
\description{
\code{att_dose()} implements the two-period framework of Callaway,
Goodman-Bacon, and Sant'Anna (2024) for a dose \code{D >= 0} with untreated
units at \code{D = 0}. It reports level effects \code{ATT(d | d)}, the effect of
dose \code{d} among units that received \code{d}, which parallel trends against the
untreated identifies, and causal responses, the slope of the dose-response
curve, which need the stronger assumption that every dose group would have
followed the same trend at every dose ("strong parallel trends"). It also
returns the two weight decompositions of the two-way fixed effects
coefficient, on level effects and on causal-response increments.
}
\details{
The panel must have exactly two periods. The two-way fixed
effects coefficient equals \code{Cov(dY, D) / Var(D)}, which is a weighted
sum of level effects with weights \verb{(d_k - E[D]) p_k / Var(D)} that can
be negative, and a weighted sum of causal-response increments with
non-negative weights \code{P(D >= d_k) (E[D | D >= d_k] - E[D]) / Var(D)}
that integrate to one. The two readings of the same coefficient rely on
different assumptions; the function reports both weight sets so the
reader can see which interpretation is being invoked.
}
\examples{
set.seed(1)
n <- 600
dose <- ifelse(runif(n) < 0.3, 0, runif(n, 0.5, 3))
dat <- data.frame(id = rep(1:n, each = 2), time = rep(1:2, n),
                  dose = rep(dose, each = 2))
dat$y <- rnorm(2 * n) + rep(rnorm(n), each = 2) + 0.5 * dat$time +
  (dat$time == 2) * (2 * dat$dose - 0.3 * dat$dose^2)
att_dose(dat, id = "id", time = "time", y = "y", dose = "dose", n_bins = 4)
}
\references{
Callaway, B., Goodman-Bacon, A., and Sant'Anna, P. H. C.
(2024). Difference-in-differences with a continuous treatment. Working
paper, arXiv:2107.02637.
}
