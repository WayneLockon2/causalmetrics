% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dr-scores-multi.R
\name{contrast_scores}
\alias{contrast_scores}
\title{Pairwise scores from a multi-arm score object}
\usage{
contrast_scores(x, treat, control)
}
\arguments{
\item{x}{A \code{cm_scores_multi} object.}

\item{treat, control}{Arm labels.}
}
\value{
A \code{cm_scores} object on the rows of the two arms, with \code{d = 1}
for \code{treat}.
}
\description{
Builds the binary \code{cm_scores} object of one arm against another from the
nuisances stored in a multi-arm object: the rows of the two arms, the
pairwise propensity \code{e_t / (e_t + e_c)}, the two outcome models, and the
doubly robust pseudo-outcome of \code{\link[=dr_scores]{dr_scores()}}. The result works with
every binary function (\code{\link[=cate_learner]{cate_learner()}}, \code{\link[=cate_blp]{cate_blp()}},
\code{\link[=cate_validate]{cate_validate()}}, \code{\link[=policy_learn]{policy_learn()}}).
}
\examples{
dat <- sim_hte(900, dgp = "smooth", seed = 1)
dat$arm <- sample(c("a", "b", "c"), 900, replace = TRUE)
dat$y3 <- dat$y + (dat$arm == "b") * dat$tau_true
sc <- dr_scores(dat, "y3", "arm", paste0("x", 1:5), seed = 1)
contrast_scores(sc, "b", "a")
}
