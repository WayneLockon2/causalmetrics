% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate-learner.R
\name{cate_learner}
\alias{cate_learner}
\title{Meta-learners for the conditional average treatment effect}
\usage{
cate_learner(
  data = NULL,
  y = NULL,
  d = NULL,
  x = NULL,
  x_het = x,
  method = c("dr", "r", "x", "t", "s"),
  learner = NULL,
  learner_p = NULL,
  learner_final = NULL,
  scores = NULL,
  folds = 5L,
  seed = NULL,
  p_clip = c(0.01, 0.99),
  interactions = FALSE,
  adapt = FALSE
)
}
\arguments{
\item{data, y, d, x}{Data frame, outcome, treatment, and covariate names, as
in \code{\link[=dr_scores]{dr_scores()}}. Not needed when \code{scores} is supplied.}

\item{x_het}{Character vector of heterogeneity variables (default \code{x}).}

\item{method}{One of \code{"dr"}, \code{"r"}, \code{"x"}, \code{"t"}, \code{"s"}.}

\item{learner}{\code{mlr3} regression learner for every first-stage outcome
regression (default linear regression).}

\item{learner_p}{\code{mlr3} classification learner for the propensity score
(default logistic regression).}

\item{learner_final}{\code{mlr3} regression learner for the final stage
(default \code{learner}). The R-learner needs a learner that accepts
observation weights (linear models, ranger, glmnet, rpart do).}

\item{scores}{Optional \code{cm_scores} object; its nuisances, folds, and data
are reused and \code{data}, \code{y}, \code{d}, \code{x} are taken from it.}

\item{folds, seed, p_clip}{Cross-fitting settings passed to \code{\link[=dr_scores]{dr_scores()}}.}

\item{interactions}{S-learner only: add treatment-covariate interactions.}

\item{adapt}{X-learner only: use the covariate-shift weights.}
}
\value{
A list of class \code{cm_cate} with \code{tau_hat} (fitted CATE on the
estimation rows), \code{label} and \code{weights} (the final-stage pseudo-label and
weights), \code{final} (the fitted final stage), \code{x_het}, \code{method}, \code{scores},
and \code{learners}. Methods: \code{\link[=predict]{predict()}} on new data, \code{\link[=print]{print()}}, \code{\link[generics:tidy]{tidy()}}.
}
\description{
Estimates the CATE \verb{tau(x) = E[Y(1) - Y(0) | X = x]} by reducing the
problem to regressions solved by \code{mlr3} learners (Künzel et al. 2019;
Nie and Wager 2021; Kennedy 2023; Chernozhukov et al. 2026, ch. 15).
Each method produces a cross-fitted pseudo-label \code{L_i} (and, for the
R-learner, weights \code{W_i}) and then fits \code{learner_final} to \code{L_i} on the
heterogeneity variables \code{x_het} using all rows. The fitted final stage
predicts on new data.
}
\details{
\itemize{
\item \code{"dr"} (DR-learner): \code{L = Y(eta)}, the doubly robust pseudo-outcome of
\code{\link[=dr_scores]{dr_scores()}}. Converges to the best approximation of the CATE in the
final learner's class; error bounded by the product of the two nuisance
errors.
\item \code{"r"} (R-learner): weighted regression of \code{(Y - l(Z)) / (D - p(Z))} on
\code{x_het} with weights \code{(D - p(Z))^2}, which minimizes the Robinson loss
\code{E[(Y - l - tau(X)(D - p))^2]}. Never divides by the propensity; targets
the overlap-weighted projection of the CATE.
\item \code{"x"} (X-learner): imputed effects \code{Y - mu0(Z)} on the treated and
\code{mu1(Z) - Y} on the controls, each regressed on \code{Z} within its arm, blended
with weights \code{1 - p(Z)} and \code{p(Z)}. With \code{adapt = TRUE} the arm-specific
stages are trained with the covariate-shift weights \code{(1 - p)^2 / p} and
\code{p^2 / (1 - p)} (the domain-adapted X-learner).
\item \code{"t"} (T-learner): \code{L = mu1(Z) - mu0(Z)} from two outcome regressions.
\item \code{"s"} (S-learner): \code{L = g(1, Z) - g(0, Z)} from one regression of \code{Y}
on \verb{(D, Z)}; \code{interactions = TRUE} adds \code{D * Z} columns.
}
}
\examples{
dat <- sim_hte(800, dgp = "smooth", seed = 1)
x <- paste0("x", 1:5)
fit <- cate_learner(dat, "y", "d", x, x_het = c("x1", "x2"), method = "dr", seed = 1)
fit
head(predict(fit, dat))
cor(fit$tau_hat, dat$tau_true)
}
\references{
Künzel, S. R., Sekhon, J. S., Bickel, P. J., and Yu, B. (2019).
Metalearners for estimating heterogeneous treatment effects using machine
learning. \emph{PNAS}, 116(10), 4156-4165.

Nie, X. and Wager, S. (2021). Quasi-oracle estimation of heterogeneous
treatment effects. \emph{Biometrika}, 108(2), 299-319.

Kennedy, E. H. (2023). Towards optimal doubly robust estimation of
heterogeneous causal effects. \emph{Electronic Journal of Statistics}, 17(2).

Foster, D. J. and Syrgkanis, V. (2023). Orthogonal statistical learning.
\emph{Annals of Statistics}, 51(3), 879-908.
}
\seealso{
\code{\link[=dr_scores]{dr_scores()}}, \code{\link[=cate_score]{cate_score()}}, \code{\link[=cate_validate]{cate_validate()}}
}
