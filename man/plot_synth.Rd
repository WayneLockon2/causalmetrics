% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-control.R
\name{plot_synth}
\alias{plot_synth}
\title{Plot a synthetic control fit or its inference objects}
\usage{
plot_synth(x, type = NULL, mspe_limit = NULL)
}
\arguments{
\item{x}{A \code{cm_synth}, \code{cm_synth_placebo}, \code{cm_synth_loo}, or
\code{cm_synth_spec} object.}

\item{type}{For \code{cm_synth}: \code{"path"} (treated and synthetic series),
\code{"gap"}, \code{"demeaned_path"} (both series after subtracting the
cross-sectional mean of all units in each period, the fit picture of
Ferman and Pinto 2021), \code{"weights"}, \code{"balance"} (standardized
predictor values of the treated unit, the synthetic unit, and the donor
mean), or \code{"event"} (separate mode: average gap by event time). For
\code{cm_synth_placebo}: \code{"placebos"} (gap paths) or \code{"ratio"} (post/pre
MSPE ratios). For \code{cm_synth_loo}: \code{"paths"}. For \code{cm_synth_spec}:
\code{"paths"} (demeaned synthetic control against DiD).}

\item{mspe_limit}{For \code{"placebos"}, drop placebos whose pre-treatment MSPE
exceeds this multiple of the treated unit's (default: the limit stored
in the object).}
}
\value{
A \code{ggplot} object.
}
\description{
Plot a synthetic control fit or its inference objects
}
