% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssiv.R
\name{ssiv_rotemberg}
\alias{ssiv_rotemberg}
\title{Rotemberg weights of a shift-share instrument}
\usage{
ssiv_rotemberg(data, y, d, shares, shocks, x = NULL, weights = NULL)
}
\arguments{
\item{data}{A data frame with one row per unit.}

\item{y, d}{Outcome and treatment column names.}

\item{shares}{Character vector of share column names (one per shock).}

\item{shocks}{Numeric vector of shocks, in the order of \code{shares}.}

\item{x}{Optional control column names.}

\item{weights}{Optional weights column name.}
}
\value{
A list of class \code{cm_ssiv_rotemberg} with \code{table} (shock,
share mean, shock value, \code{alpha_k}, \code{beta_k}, first-stage t), the 2SLS
\code{estimate} and \code{std.error}, \code{check} (\verb{sum(alpha_k beta_k)}), and
\code{summary} (share of negative weights, sum of negative weights, top
five weights, correlation of weights with shocks).
}
\description{
A shift-share (Bartik) instrument \verb{B_i = sum_k s_ik g_k} combines unit
exposure shares \code{s_ik} with shocks \code{g_k}. Goldsmith-Pinkham, Sorkin, and
Swift (2020) show that the two-stage least squares coefficient equals a
weighted average of the just-identified estimates that use each share as
the instrument, \verb{beta = sum_k alpha_k beta_k}, with Rotemberg weights
\verb{alpha_k = g_k (Z_k' D^perp) / sum_j g_j (Z_j' D^perp)}, where \code{Z_k} is
the residualized share and \code{D^perp} the residualized treatment. The
weights say which industries (shocks) drive the estimate and are the
object to inspect when the identifying assumption is exogeneity of the
shares.
}
\examples{
dat <- sim_iv(400, dgp = "shift_share", n_industries = 10, seed = 1)
ssiv_rotemberg(dat, "y", "d", shares = paste0("s", 1:10), shocks = attr(dat, "shocks"), x = "x1")
}
\references{
Goldsmith-Pinkham, P., Sorkin, I., and Swift, H. (2020). Bartik
instruments: what, when, why, and how. \emph{American Economic Review},
110(8), 2586-2624.
}
