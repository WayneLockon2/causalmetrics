% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/est-dml-structured.R
\name{est_dml_structured}
\alias{est_dml_structured}
\title{Double machine learning for a structured outcome model}
\usage{
est_dml_structured(
  theta_hat,
  y,
  t_obs,
  targets,
  t0 = NULL,
  link = c("gen_sigmoid", "linear", "logit", "custom"),
  G = NULL,
  G_grad = NULL,
  t_dist = NULL,
  fold_id = NULL,
  ridge = 0,
  conf_level = 0.95,
  contrast_best = TRUE
)
}
\arguments{
\item{theta_hat}{Numeric matrix, \code{n} rows, one column per nuisance
parameter, of out-of-fold estimates of \code{theta(x_i)}.}

\item{y}{Outcome vector.}

\item{t_obs}{Realized treatment combinations: an \verb{n x m} 0/1 matrix (or a
data frame of indicator columns).}

\item{targets}{Treatment combinations whose effect against \code{t0} is wanted:
a \verb{K x m} 0/1 matrix, a data frame, or a character vector such as
\code{c("110", "101")}.}

\item{t0}{Reference combination (default all zeros).}

\item{link}{\code{"gen_sigmoid"} (generalized sigmoid form II of Ye et al.:
\verb{theta = (theta_0, theta_1..theta_m, c)}, \verb{G = c / (1 + exp(-(theta_0 + theta' t)))}), \code{"linear"} (\verb{theta_0 + theta' t}), \code{"logit"}
(\verb{1 / (1 + exp(-(theta_0 + theta' t)))}, Farrell et al. 2020), or
\code{"custom"} with \code{G} and \code{G_grad}.}

\item{G, G_grad}{For \code{link = "custom"}: functions \verb{(theta, t)} where \code{theta}
is the \verb{n x d} matrix and \code{t} a single combination (length \code{m}) or an
\verb{n x m} matrix, returning a length-\code{n} vector and an \verb{n x d} gradient
matrix.}

\item{t_dist}{Assignment distribution of the observed combinations used in
\code{Lambda(x)}: a data frame with the \code{m} indicator columns and a \code{prob}
column, a matrix with a \code{prob} attribute, or \code{NULL} for the empirical
shares of \code{t_obs}. Probabilities that do not sum to one are accepted with
a warning and used as weights (this reproduces some published code).}

\item{fold_id}{Optional fold identifier of the cross-fitting that produced
\code{theta_hat}; estimates are reported fold by fold and pooled.}

\item{ridge}{Added to the diagonal of \code{Lambda(x)} before inversion (the
device used in the authors' code; the theory uses 0).}

\item{conf_level}{Confidence level.}

\item{contrast_best}{Also estimate \verb{tau(t) = mu(t*) - mu(t)}, the gap to
the best target, from the difference of the influence functions.}
}
\value{
A list of class \code{cm_dml_structured}: \code{estimates} (target, fold
including \code{"pooled"}, n, \code{estimate}, \code{plugin}, \code{std.error}, \code{conf.low},
\code{conf.high}, and for the pooled rows \code{ci_low_avg}, \code{ci_high_avg}, the
averages of the fold-level bounds as in Ye et al.'s Table 7),
\code{contrasts} (the gap to the best target), \code{psi} (\verb{n x K} influence
functions, for group averages), \code{plugin} (\verb{n x K} plug-in terms),
\code{best}, \code{targets}, \code{t_dist}, \code{fold_id}, \code{diagnostics} (share of rows whose
\code{Lambda(x)} was ill conditioned), and the settings. Methods: \code{\link[=print]{print()}},
\code{\link[generics:tidy]{tidy()}}.
}
\description{
Estimates average treatment effects of treatment combinations from a
structured model \verb{E[Y | X = x, T = t] = G(theta(x), t)} with a known link
\code{G} and a nuisance parameter function \code{theta(x)} estimated out of fold by
any learner (a deep network, a forest, a regression). The estimator is the
cross-fitted mean of the influence function of Farrell, Liang, and Misra
(2020), adapted by Ye et al. (2025) to combinatorial experiments:
\deqn{\psi_i(t) = H(x_i, \hat\theta; t, t_0) - H_\theta(x_i, \hat\theta; t, t_0)'
  \Lambda(x_i)^{-1} \ell_\theta(y_i, \check t_i, \hat\theta),}
with \code{H = G(theta, t) - G(theta, t0)}, \code{H_theta} its gradient in \code{theta},
\verb{Lambda(x) = 2 sum_t nu(t) G_theta(theta, t) G_theta(theta, t)'} over the
known assignment distribution \code{nu} of the observed combinations, and
\verb{l_theta = 2 G_theta(theta, t_obs) (G(theta, t_obs) - y)} the gradient of the
squared loss. The first term alone is the plug-in (structured learning)
estimator; the second term removes the first-order effect of nuisance
estimation error (Neyman orthogonality), so the mean of \code{psi} is
root-n normal when \code{theta_hat} converges faster than \code{n^-1/4}.
}
\details{
The pooled estimate is the mean of the fold estimates. Its
standard error treats the folds as independent, \verb{sqrt(sum se_s^2) / S},
which coincides with the influence-function standard error of the
stacked \code{psi} when the folds are of equal size. \code{ci_low_avg} and
\code{ci_high_avg} average the fold bounds instead and are wider by about
\code{sqrt(S)}; they are reported because the paper's tables use them.
}
\examples{
set.seed(1)
n <- 2000; m <- 2
x <- matrix(runif(n * 3), n)
theta <- cbind(x[, 1] - 0.5, 1 + x[, 2], -0.5 + x[, 3], 4)
t_obs <- cbind(rbinom(n, 1, 0.5), rbinom(n, 1, 0.5))
u <- theta[, 1] + theta[, 2] * t_obs[, 1] + theta[, 3] * t_obs[, 2]
y <- theta[, 4] * plogis(u) + rnorm(n, sd = 0.1)
# with the true theta the correction has mean zero and the estimator is the
# plug-in contrast
fit <- est_dml_structured(theta, y, t_obs, targets = c("10", "01", "11"))
fit
}
\references{
Farrell, M. H., Liang, T., and Misra, S. (2020). Deep learning for
individual heterogeneity: an automatic inference framework.
arXiv:2010.14694.

Ye, Z., Zhang, Z., Zhang, D. J., Zhang, H., and Zhang, R. (2025). Deep
learning-based causal inference for large-scale combinatorial experiments:
theory and empirical evidence. \emph{Management Science}.
}
