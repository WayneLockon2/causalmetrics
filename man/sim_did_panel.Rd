% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-did-panel.R
\name{sim_did_panel}
\alias{sim_did_panel}
\title{Simulate a staggered-adoption panel}
\usage{
sim_did_panel(
  n_units = 500,
  n_periods = 10,
  groups = c(4, 7),
  never_share = 0.4,
  mu = c(3, 2, 1),
  effect = NULL,
  x_trend = 0,
  x_select = 0.5,
  anticipation = 0,
  sd_e = 1,
  seed = NULL
)
}
\arguments{
\item{n_units}{Number of units.}

\item{n_periods}{Number of periods \verb{1, ..., n_periods}.}

\item{groups}{First treatment periods of the cohorts.}

\item{never_share}{Share of units never treated.}

\item{mu}{Cohort-specific effect sizes, recycled over \code{groups}. The
effect at event time \code{e >= 0} is \code{mu[g] * (e + 1)} by default.}

\item{effect}{Optional function \code{effect(e, g)} returning the treatment
effect at event time \code{e} for cohort \code{g}; overrides \code{mu}.}

\item{x_trend}{Slope of the covariate-specific trend: untreated outcomes
grow by \code{x_trend * x1} per period, so parallel trends holds only
conditional on \code{x1} when \code{x_trend != 0}.}

\item{x_select}{Strength of selection into earlier cohorts on \code{x1}.}

\item{anticipation}{Number of periods before \code{g} in which half of the
eventual first-period effect already appears.}

\item{sd_e}{Standard deviation of the idiosyncratic error.}

\item{seed}{Optional seed.}
}
\value{
A data frame in long format with \code{id}, \code{time}, \code{g} (0 for never
treated), \code{treated} (1 once treated), \code{event_time}, \code{x1}, \code{x2}, \code{y}, and
\code{tau} (the realized treatment effect, 0 when untreated). The attribute
\code{"att_gt"} holds the population \code{ATT(g, t)} implied by the design.
}
\description{
A data-generating process for teaching and testing difference-in-differences
estimators: units are assigned to treatment cohorts (first treatment period
\code{g}) or never treated, potential outcomes have unit and time effects,
optional covariate-specific trends, and treatment effects that grow with
exposure and differ across cohorts, as in the stylized example of
Sant'Anna's lectures.
}
\examples{
dat <- sim_did_panel(n_units = 200, n_periods = 6, groups = c(3, 5), seed = 1)
head(dat)
attr(dat, "att_gt")
}
