% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-inference.R
\name{synth_loo}
\alias{synth_loo}
\title{Leave-one-donor-out synthetic controls}
\usage{
synth_loo(x, min_weight = 0.001)
}
\arguments{
\item{x}{A \code{cm_synth} object in the average mode.}

\item{min_weight}{Donors with weight at or below this value are not
dropped (default 0.001, as in Andersson 2019).}
}
\value{
An object of class \code{cm_synth_loo} with \code{table} (dropped donor,
its weight, the new estimate and pre-treatment RMSPE) and \code{paths} (the
synthetic paths, one column per refit).
}
\description{
Refits the synthetic control after dropping, one at a time, each donor
with weight above \code{min_weight}.
}
\examples{
dat <- sim_synth_panel(n_donors = 10, t_pre = 12, t_post = 4, effect = 2, seed = 5)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d")
synth_loo(fit)$table
}
