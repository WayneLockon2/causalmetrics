% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediate-dml.R
\name{mediate_dml}
\alias{mediate_dml}
\title{Double machine learning for causal mediation}
\usage{
mediate_dml(
  data,
  y,
  d,
  m,
  x,
  learner_y = NULL,
  learner_d = NULL,
  learner_nu = NULL,
  learner_m = NULL,
  m_ref = NULL,
  folds = 5L,
  n_rep = 1L,
  trim = 0.01,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d}{Outcome and binary treatment column names.}

\item{m}{Character vector of mediator names.}

\item{x}{Character vector of covariate names.}

\item{learner_y}{\code{mlr3} learner for the outcome regressions (default
linear regression, or logistic regression for a binary outcome).}

\item{learner_d}{\code{mlr3} classification learner for \code{P(D = 1 | X)} and
\code{P(D = 1 | M, X)} (default logistic regression).}

\item{learner_nu}{\code{mlr3} regression learner for the nested regression
(default \code{learner_y}, or linear regression when that is a classifier).}

\item{learner_m}{\code{mlr3} classification learner for \code{P(M = 1 | D, X)},
used only for the controlled direct effect with a binary mediator.}

\item{m_ref}{Mediator level for the controlled direct effect (binary
mediator only); \code{NULL} skips it.}

\item{folds}{Number of cross-fitting folds.}

\item{n_rep}{Repeated cross-fitting partitions (median aggregation as in
\code{\link[=est_dml]{est_dml()}}).}

\item{trim}{Observations whose estimated propensities \code{p_1(X)} or
\code{p_1(M, X)} fall outside \verb{[trim, 1 - trim]} are dropped from the score
(their share is reported).}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed for the folds.}
}
\value{
A list of class \code{cm_med_dml} with \code{effects} (total, nde, nie,
nde_total, nie_pure, and cde when requested), \code{potential} (the four
\verb{E[Y(d, M(d'))]} with standard errors), \code{psi} (the influence functions
of the representative repetition), \code{diagnostics} (nuisance fit,
propensity summaries, share trimmed), \code{learners}, and settings.
}
\description{
Estimates \verb{E[Y(d, M(d'))]} for the four combinations of a binary treatment
\code{d} and the mediator's counterfactual \verb{M(d')}, hence the total effect, the
natural direct and indirect effects (both decompositions), and optionally
the controlled direct effect at a mediator level, using the efficient
influence function under sequential ignorability:
\deqn{\psi_{d,d'} = \frac{1\{D = d\}\, p_{d'}(M, X)}{p_d(M, X)\, p_{d'}(X)}\,(Y - \mu_d(M, X))
  + \frac{1\{D = d'\}}{p_{d'}(X)}\,(\mu_d(M, X) - \nu_{d,d'}(X)) + \nu_{d,d'}(X),}
with \verb{mu_d(M, X) = E[Y | D = d, M, X]}, \code{p_d(X) = P(D = d | X)},
\code{p_d(M, X) = P(D = d | M, X)}, and the nested regression
\verb{nu_\{d,d'\}(X) = E[mu_d(M, X) | D = d', X]}. For \verb{d = d'} the score is the
AIPW score of the average potential outcome. The mediator-density ratio
\verb{f(M | d', X) / f(M | d, X)} is written through the two propensities, so
the mediator may be binary, continuous, or a vector. All nuisances are
cross-fitted; the nested regression is trained on the units with \verb{D = d'}
of the training folds using the outcome model fitted on the units with
\code{D = d} of the same folds, so no observation's label enters its own
nuisance.
}
\examples{
dat <- sim_mediation(1500, dgp = "linear", seed = 1)
fit <- mediate_dml(dat, "y", "d", "m", x = c("x1", "x2"), seed = 1)
fit
}
\references{
Tchetgen Tchetgen, E. J. and Shpitser, I. (2012). Semiparametric theory
for causal mediation analysis. \emph{Annals of Statistics}, 40(3), 1816-1845.

Farbmacher, H., Huber, M., Laffers, L., Langen, H., and Spindler, M.
(2022). Causal mediation analysis with double machine learning.
\emph{The Econometrics Journal}, 25(2), 277-300.
}
\seealso{
\code{\link[=mediate_reg]{mediate_reg()}}, \code{\link[=est_dml]{est_dml()}}
}
