% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv-weak.R
\name{iv_plausibly_exogenous}
\alias{iv_plausibly_exogenous}
\title{Plausibly exogenous instruments: union of intervals}
\usage{
iv_plausibly_exogenous(
  data,
  y,
  d,
  z,
  x = NULL,
  gamma_grid = seq(-0.5, 0.5, by = 0.1),
  cluster = NULL,
  weights = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{Outcome column name.}

\item{d}{Endogenous regressor column name.}

\item{z}{Character vector of instrument column names.}

\item{x}{Optional character vector of exogenous control column names.}

\item{gamma_grid}{Numeric vector of direct effects to entertain.}

\item{cluster}{Optional cluster column name.}

\item{weights}{Optional weights column name.}

\item{conf_level}{Confidence level.}
}
\value{
A data frame with one row per \code{gamma} (estimate, standard error,
interval) and attributes \code{union} (the overall interval) and
\code{gamma_zero} (the interval at \code{gamma = 0}).
}
\description{
Conley, Hansen, and Rossi (2012): allow the instrument a direct effect
\code{gamma} on the outcome, \verb{Y = theta D + gamma Z + ...}. For each \code{gamma} in
\code{gamma_grid}, subtract \verb{gamma Z} from \code{Y} and re-estimate by two-stage least
squares; the union of the resulting confidence intervals is a confidence
set that is valid if the true \code{gamma} lies in the grid.
}
\examples{
dat <- sim_iv(1000, dgp = "linear", seed = 1)
iv_plausibly_exogenous(dat, "y", "d", "z", paste0("x", 1:5), gamma_grid = seq(-0.2, 0.2, by = 0.1))
}
\references{
Conley, T. G., Hansen, C. B., and Rossi, P. E. (2012). Plausibly exogenous.
\emph{Review of Economics and Statistics}, 94(1), 260-272.
}
