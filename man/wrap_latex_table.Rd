% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-task.R
\name{wrap_latex_table}
\alias{wrap_latex_table}
\title{Wrap a kable in a LaTeX table environment}
\usage{
wrap_latex_table(
  tbl,
  caption = NULL,
  notes = NULL,
  label = NULL,
  width = NULL,
  height = NULL,
  path = NULL
)
}
\arguments{
\item{tbl}{A kable or kable_styling object, ideally from \code{table_task()}.}

\item{caption}{Character. Table caption. Default \code{NULL}.}

\item{notes}{Character. Italicized notes line below the table.}

\item{label}{Character. LaTeX label, e.g., \code{"tab:summary"}.}

\item{width}{Numeric or NULL. Table width as a fraction of \verb{\\\\textwidth}.
If \code{NULL}, no resizebox is applied and the table renders at its natural
width. Default \code{NULL}.}

\item{height}{Numeric or NULL. Table height as a fraction of
\verb{\\\\textheight}. Used only if \code{width} is provided. Default \code{NULL}
(preserves aspect from width).}

\item{path}{Character or NULL. If provided, the inner tabular is written
to this \code{.tex} file and the LaTeX block uses \verb{\\\\input\{path\}}. If \code{NULL},
the tabular is embedded inline. Default \code{NULL}.}
}
\value{
Character string of LaTeX code.
}
\description{
Takes a finished kable and produces a complete \verb{\\\\begin\{table\}} block with
caption, optional resizebox, and a notes line styled as
\verb{\{\\\\footnotesize \\\\textit\{Notes\}: ...\}}. Use after \code{table_task()} once the
table content is finalized.
}
\examples{
\dontrun{
dt <- data.frame(x = 1:3, y = c(2.1, 3.4, 4.5))
tbl <- table_task(dt, digits = 2)

# Simple wrap, no resizebox:
wrap_latex_table(tbl, caption = "Example", label = "tab:ex")

# With resizebox and notes:
wrap_latex_table(
  tbl,
  caption = "Example",
  notes   = "Some explanation here.",
  label   = "tab:ex",
  width   = 0.8
)
}

}
