% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mediate-reg.R
\name{mediate_sensitivity}
\alias{mediate_sensitivity}
\title{Sensitivity of the natural indirect effect to an unmeasured mediator-outcome confounder}
\usage{
mediate_sensitivity(fit, rho = seq(-0.9, 0.9, by = 0.05))
}
\arguments{
\item{fit}{A \code{cm_mediation} object from \code{\link[=mediate_reg]{mediate_reg()}}.}

\item{rho}{Grid of error correlations.}
}
\value{
A list of class \code{cm_med_sensitivity} with \code{curve} (rho, nie), the
value \code{rho_zero} at which the indirect effect crosses zero, and the
implied \code{rho_tilde}.
}
\description{
For a \code{cm_mediation} object fitted with one mediator, linear models, and no
interaction, computes the natural indirect effect as a function of the
correlation \code{rho} between the errors of the mediator model and the outcome
model (Imai, Keele, and Yamamoto 2010, Theorem 3):
\deqn{NIE(\rho) = \beta_2 \frac{\sigma_1}{\sigma_2}
  \Big(\tilde\rho - \rho \sqrt{\frac{1 - \tilde\rho^2}{1 - \rho^2}}\Big),}
where \code{beta_2} is the treatment effect on the mediator, \code{sigma_1} and
\code{sigma_2} the residual standard deviations of the total-effect regression
\code{Y ~ D + X} and of the mediator regression, and \verb{tilde rho} the
correlation of their residuals. \code{rho = 0} is sequential ignorability.
}
\examples{
dat <- sim_mediation(1000, dgp = "correlated_errors", rho = 0.4, seed = 1)
fit <- mediate_reg(dat, "y", "d", "m", x = c("x1", "x2"), seed = 1)
sens <- mediate_sensitivity(fit)
sens$rho_zero
}
