% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-inference.R
\name{synth_spec_test}
\alias{synth_spec_test}
\title{Specification test of Ferman and Pinto (2021)}
\usage{
synth_spec_test(x, q = 2)
}
\arguments{
\item{x}{A \code{cm_synth} object in the average mode.}

\item{q}{Norm for the secondary statistic \verb{(mean |u_t|^q)^(1/q)}.}
}
\value{
A list of class \code{cm_synth_spec} with \code{p_value} (statistic
\verb{|mean of u_t over post periods|}), \code{p_value_norm} (the \code{q}-norm
statistic), \code{u} (the contrast series), and \code{paths} (the demeaned
synthetic control and DiD counterfactuals fitted on the pre-treatment
periods, for the plot).
}
\description{
Contrasts the demeaned synthetic control with difference-in-differences.
Under a linear factor model both are unbiased when the common factors
have mean zero after treatment; a large difference between them signals
that both are biased. The test imposes the null, fits the demeaned
synthetic control with every period's outcome as a predictor, forms
\verb{u_t = w'(y_t - ybar) - (1/J) 1'(y_t - ybar)} (Ferman and Pinto, eq. 11),
and compares the post-treatment average of \code{u_t} with its moving-block
permutations (their Proposition 4).
}
\examples{
dat <- sim_synth_panel(n_donors = 12, t_pre = 15, t_post = 4, effect = 3, seed = 4)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d", demean = TRUE)
synth_spec_test(fit)
}
