% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv-compliers.R
\name{complier_profile}
\alias{complier_profile}
\title{Complier share and characteristics (Abadie's kappa)}
\usage{
complier_profile(
  data,
  d,
  z,
  covariates,
  x = NULL,
  p_hat = NULL,
  n_boot = 199L,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{d, z}{Binary treatment and binary instrument column names.}

\item{covariates}{Character vector of covariates to profile.}

\item{x}{Optional covariates for the instrument propensity (logistic
regression); \code{NULL} uses the unconditional share of \code{Z = 1}.}

\item{p_hat}{Optional supplied \code{P(Z = 1 | X)} (vector or column name).}

\item{n_boot}{Bootstrap replications for standard errors (0 to skip).}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_compliers} with \code{shares} (complier,
always-taker, never-taker shares with standard errors), \code{table}
(covariate means by group, complier/population ratios, standard errors
of the complier means), \code{kappa} (the weights), and the propensity.
}
\description{
Under the LATE assumptions with a binary instrument \code{Z} and binary
treatment \code{D}, Abadie (2003) shows that the weights
\verb{kappa = 1 - D (1 - Z) / (1 - pi(X)) - (1 - D) Z / pi(X)}, with
\code{pi(X) = P(Z = 1 | X)}, average to the complier share and that
\verb{E[kappa g(X)] / E[kappa]} is the complier mean of any function of the
covariates. Always-takers are the units with \verb{D = 1, Z = 0} (weighted by
\code{1 / (1 - pi)}) and never-takers those with \verb{D = 0, Z = 1} (weighted by
\code{1 / pi}). The function reports the three group shares and the covariate
means of each group next to the full-sample means, with bootstrap standard
errors that re-estimate the instrument propensity.
}
\examples{
dat <- sim_iv(2000, dgp = "late", seed = 1)
complier_profile(dat, d = "d", z = "z", covariates = c("x1", "x2"), x = "x1", n_boot = 49)
}
\references{
Abadie, A. (2003). Semiparametric instrumental variable estimation of
treatment response models. \emph{Journal of Econometrics}, 113(2), 231-263.
}
