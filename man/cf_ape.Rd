% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control-function.R
\name{cf_ape}
\alias{cf_ape}
\title{Average partial effects from a control-function second stage}
\usage{
cf_ape(fit, data, d, delta = NULL, h = NULL)
}
\arguments{
\item{fit}{The fitted second stage.}

\item{data}{The augmented data used to fit it.}

\item{d}{Name of the endogenous regressor.}

\item{delta}{Optional discrete change in \code{d} (default: derivative).}

\item{h}{Step for the numerical derivative (default \verb{1e-4 sd(d)}).}
}
\value{
A named numeric vector with the APE (name \code{paste0("ape_", d)}) and
attribute \code{"contributions"} holding the per-observation effects.
}
\description{
For a fitted nonlinear second stage (a \code{glm} with a probit, logit, or
Poisson family, or any model with a \code{predict(newdata, type = "response")}
method), computes the average partial effect of \code{d} from the average
structural function: the derivative of the mean prediction with respect
to \code{d}, averaged over the observed covariates and the estimated control,
or the mean difference of predictions for a discrete change \code{delta}.
Averaging over the control is what makes the quantity structural
(Blundell and Powell 2004; Wooldridge 2015).
}
