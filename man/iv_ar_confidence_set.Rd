% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iv-weak.R
\name{iv_ar_confidence_set}
\alias{iv_ar_confidence_set}
\title{Anderson-Rubin confidence set for an instrumental-variable coefficient}
\usage{
iv_ar_confidence_set(
  data,
  y,
  d,
  z,
  x = NULL,
  cluster = NULL,
  weights = NULL,
  conf_level = 0.95,
  theta_grid = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y}{Outcome column name.}

\item{d}{Endogenous regressor column name.}

\item{z}{Character vector of instrument column names.}

\item{x}{Optional character vector of exogenous control column names.}

\item{cluster}{Optional cluster column name.}

\item{weights}{Optional weights column name.}

\item{conf_level}{Confidence level.}

\item{theta_grid}{Grid of candidate values (several instruments); by
default a wide interval around the two-stage least squares estimate.}
}
\value{
A list of class \code{cm_ar_set} with \code{intervals} (a data frame of
lower and upper limits; possibly several rows or infinite limits),
\code{type} (\code{"bounded"}, \code{"unbounded"}, \code{"whole line"}, \code{"empty"}), the
two-stage least squares \code{estimate} and \code{std.error} with their Wald
interval for comparison, \code{statistic} (the grid, when used), and
\code{crit_val}. \code{\link[=plot_ar_set]{plot_ar_set()}} draws the statistic against \code{theta}.
}
\description{
Inverts the Anderson-Rubin test with a heteroskedasticity- or
cluster-robust variance: for each candidate \code{theta} the statistic
\verb{C(theta) = n M(theta)' Omega(theta)^-1 M(theta)} with
\verb{M(theta) = mean[(Y - theta D) Z]} (after partialling out the controls) is
compared with a chi-square critical value with as many degrees of freedom
as instruments. The set of non-rejected values is valid whatever the
strength of the instrument, and may be unbounded or empty. With one
instrument the set is computed analytically; with several, on a grid.
The same statistic is Neyman's C(alpha) test, which is why it also applies
to the cross-fitted residuals of \code{\link[=est_dml]{est_dml()}} (\code{weak_iv = TRUE}).
}
\examples{
dat <- sim_iv(500, dgp = "weak", seed = 1)
iv_ar_confidence_set(dat, y = "y", d = "d", z = "z", x = paste0("x", 1:3))
}
\references{
Anderson, T. W. and Rubin, H. (1949). Estimation of the parameters of a
single equation in a complete system of stochastic equations. \emph{Annals of
Mathematical Statistics}, 20, 46-63.

Andrews, I., Stock, J. H., and Sun, L. (2019). Weak instruments in
instrumental variables regression: theory and practice. \emph{Annual Review of
Economics}, 11, 727-753.
}
