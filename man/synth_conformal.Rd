% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-inference.R
\name{synth_conformal}
\alias{synth_conformal}
\title{Conformal inference for a synthetic control}
\usage{
synth_conformal(
  x,
  null = 0,
  q = 1,
  permutations = c("moving_block", "iid"),
  n_perm = 999L,
  grid = NULL,
  level = 0.95,
  per_period = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{A \code{cm_synth} object in the average mode.}

\item{null}{The null effect: a scalar (constant over post periods) or a
vector with one entry per post period. Default 0.}

\item{q}{Norm of the test statistic, \verb{(mean |u_t|^q)^(1/q)} over the
post periods (default 1).}

\item{permutations}{\code{"moving_block"} (all cyclic shifts of the residual
vector; the default, valid under weak dependence) or \code{"iid"} (random
permutations).}

\item{n_perm}{Number of random permutations for \code{permutations = "iid"}.}

\item{grid}{Values of a constant effect over which to invert the test;
by default 61 points around the estimate, widened until the confidence
set no longer touches an end of the grid.}

\item{level}{Confidence level of the reported set (default 0.95).}

\item{per_period}{Also compute a p-value and a confidence set for each
post period separately.}

\item{seed}{Optional seed for \code{"iid"} permutations.}
}
\value{
A list of class \code{cm_synth_conformal} with \code{p_value} (for \code{null}),
\code{statistic}, \code{conf_set} (\code{grid} values not rejected at \code{level}, with
\code{conf.low} and \code{conf.high} as its range), \code{grid_p} (p-value at each
grid value), and, if requested, \code{per_period} (time, gap, p-value at
zero, and interval).
}
\description{
The test of Chernozhukov, Wuthrich, and Zhu (2021): under a sharp null
\code{H0: tau_t = theta_t} for the post-treatment periods, subtract the null
effect from the treated outcome, refit the synthetic control on all
periods with every period's outcome as a predictor, and compare the
post-treatment residuals with their moving-block permutations. Inverting
the test over \code{grid} gives a confidence set for a constant effect;
\code{per_period = TRUE} repeats the test period by period, dropping the
other post-treatment periods.
}
\examples{
dat <- sim_synth_panel(n_donors = 12, t_pre = 15, t_post = 4, effect = 3, seed = 3)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d")
ci <- synth_conformal(fit)
c(p_value = ci$p_value, ci$conf.low, ci$conf.high)
}
\references{
Chernozhukov, V., Wuthrich, K., and Zhu, Y. (2021). An exact
and robust conformal inference method for counterfactual and synthetic
controls. \emph{JASA}, 116(536), 1849-1864.
}
