% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mte.R
\name{mte_curve}
\alias{mte_curve}
\title{Marginal treatment effect curve and policy-relevant averages}
\usage{
mte_curve(
  data,
  y,
  d,
  z,
  x = NULL,
  method = c("polynomial", "local"),
  degree = 3L,
  bandwidth = NULL,
  grid = seq(0.05, 0.95, by = 0.05),
  late_range = NULL,
  policy = NULL,
  n_boot = 99L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d}{Outcome and binary treatment column names.}

\item{z}{Character vector of instrument column names.}

\item{x}{Optional covariate column names.}

\item{method}{\code{"polynomial"} (default) or \code{"local"} (local quadratic
derivative of the residualized outcome in \code{p}).}

\item{degree}{Polynomial degree of \code{K(p)}.}

\item{bandwidth}{Bandwidth for the local method (default
\verb{1.06 sd(p) n^-1/5}).}

\item{grid}{Values of \code{u} at which to report the curve.}

\item{late_range}{Two propensity values defining the LATE weight.}

\item{policy}{Optional function mapping the estimated propensity to the
counterfactual propensity under a policy (for the PRTE).}

\item{n_boot}{Bootstrap replications (0 to skip).}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_mte} with \code{curve} (u, mte, std.error,
conf.low, conf.high, and the support indicator), \code{effects} (ATE, ATT,
ATU, LATE, and PRTE with standard errors and the share of each weight
outside the support of the estimated propensity), \code{support} (range and
quantiles of the propensity), the fitted models, and the weight
functions on the grid. \code{\link[=plot_mte]{plot_mte()}} draws curve and weights.
}
\description{
In the selection model \verb{D = 1\{p(Z, X) > V\}} with \code{V ~ U(0, 1)}, the
marginal treatment effect \verb{MTE(u, x) = E[Y(1) - Y(0) | X = x, V = u]} is
the derivative of \verb{E[Y | X, p(Z, X) = p]} in \code{p} at \code{p = u} (local
instrumental variables; Heckman and Vytlacil 2005). The function estimates
the propensity by logistic regression of \code{D} on \verb{(Z, X)}, then fits the
separable model \verb{E[Y | X, p] = X'b0 + p X'(b1 - b0) + K(p)} with \code{K} a
polynomial in \code{p} (or a local quadratic fit of the residual on \code{p}), and
reports \verb{MTE(u) = mean(X)'(b1 - b0) + K'(u)} on a grid. The curve is then
integrated against the Heckman-Vytlacil weights: \code{1} for the ATE,
\code{P(p > u) / E[p]} for the ATT, \code{P(p <= u) / E[1 - p]} for the ATU, the
uniform weight on \verb{[p_low, p_high]} for the LATE of an instrument shift,
and \verb{(F_p(u) - F_p'(u)) / (E[p'] - E[p])} for a policy that moves the
propensity from \code{p} to \verb{p'}. Bootstrap replications re-estimate both
stages.
}
\examples{
dat <- sim_iv(3000, dgp = "mte", seed = 1)
m <- mte_curve(dat, "y", "d", "z", x = "x1", degree = 3, n_boot = 20, seed = 1)
m
}
\references{
Heckman, J. J. and Vytlacil, E. (2005). Structural equations, treatment
effects, and econometric policy evaluation. \emph{Econometrica}, 73(3),
669-738.

Cornelissen, T., Dustmann, C., Raute, A., and Schönberg, U. (2016). From
LATE to MTE: alternative methods for the evaluation of policy
interventions. \emph{Labour Economics}, 41, 47-60.
}
