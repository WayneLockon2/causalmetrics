% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-task.R
\name{wrap_latex_figure}
\alias{wrap_latex_figure}
\title{Save a ggplot and return a LaTeX figure block}
\usage{
wrap_latex_figure(
  plot,
  caption = NULL,
  notes = NULL,
  label = NULL,
  width = 0.8,
  path = NULL,
  fig_width = 7,
  fig_height = 4.5
)
}
\arguments{
\item{plot}{A ggplot object.}

\item{caption}{Character. Figure caption.}

\item{notes}{Character. Italicized notes line below the figure.}

\item{label}{Character. LaTeX label, e.g., \code{"fig:event_study"}.}

\item{width}{Numeric. Figure width as a fraction of \verb{\\\\textwidth}.
Default 0.8.}

\item{path}{Character or NULL. File path for the saved PDF. If \code{NULL}
(default), a tempfile in \code{tempdir()} is used. Tempfile output is fine
for one-shot rendering in the same R session, but for documents you
plan to re-knit later, pass an explicit path like
\code{"figures/event_study.pdf"}.}

\item{fig_width}{Numeric. PDF width in inches. Default 7.}

\item{fig_height}{Numeric. PDF height in inches. Default 4.5.}
}
\value{
Character string of LaTeX code (with class \code{cm_latex_block}).
}
\description{
Takes a composed ggplot, saves it to PDF, and returns a
\verb{\\\\begin\{figure\}} block referencing the saved file. Use after
\code{plot_task()} once you've added all geoms and labels.
}
\examples{
\dontrun{
library(ggplot2)
p <- plot_task(mtcars, aes(wt, mpg)) + geom_point()

# Quick render to tempfile
wrap_latex_figure(p, caption = "Quick look", label = "fig:quick")

# Persistent path for a paper
wrap_latex_figure(
  p,
  caption = "Final figure",
  label   = "fig:final",
  path    = "figures/final.pdf"
)
}

}
