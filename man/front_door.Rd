% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/front-door.R
\name{front_door}
\alias{front_door}
\title{Front-door adjustment}
\usage{
front_door(
  data,
  y,
  d,
  m,
  x = NULL,
  method = c("regression", "aipw"),
  learner_y = NULL,
  learner_d = NULL,
  learner_m = NULL,
  folds = 5L,
  cluster = NULL,
  n_boot = 499L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d, m}{Outcome, binary treatment, and mediator column names.}

\item{x}{Optional covariate names.}

\item{method}{\code{"regression"} (any mediator; continuous mediators require a
linear outcome model in \code{M}) or \code{"aipw"} (binary mediator).}

\item{learner_y, learner_d, learner_m}{\code{mlr3} learners for \code{method = "aipw"}
(defaults: linear/logistic regression).}

\item{folds}{Cross-fitting folds for \code{method = "aipw"}.}

\item{cluster}{Optional cluster column for the bootstrap.}

\item{n_boot}{Bootstrap replications for \code{method = "regression"}.}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_front_door} with \code{effects} (\code{total} through
the front door, \code{backdoor} for the naive regression adjustment on \code{X},
and for the regression method the two components \code{d_on_m} and
\code{m_on_y}), \code{potential} (\code{E[Y(1)]}, \code{E[Y(0)]}), and settings.
}
\description{
Under the front-door criterion (the mediator \code{M} is unconfounded given
\code{D} and \code{X}; \code{D} affects \code{Y} only through \code{M}; the \code{M}-\code{Y} relation is
unconfounded given \code{D} and \code{X}), the mean potential outcome is
\deqn{E[Y(d)] = E_X\Big[\sum_m P(m \mid d, X) \sum_{d'} E[Y \mid d', m, X]\, P(d' \mid X)\Big],}
even when unobserved variables confound \code{D} and \code{Y}. The regression
estimator fits \code{M ~ D + X} (logistic for a binary mediator) and
\code{Y ~ D + M + X} and plugs in; for a binary mediator \code{method = "aipw"}
uses the influence function
\deqn{\varphi_d = \frac{f(M \mid d, X)}{f(M \mid D, X)}\,(Y - \mu(D, M, X))
 + \frac{1\{D = d\}}{p(d \mid X)}\,(\xi(M, X) - \eta_d(X)) + \sum_m f(m \mid d, X)\,\mu(D, m, X),}
with \verb{xi(m, X) = sum_d' mu(d', m, X) p(d' | X)} and
\verb{eta_d(X) = sum_m f(m | d, X) xi(m, X)}, whose nuisances are cross-fitted
\code{mlr3} learners. Inference by bootstrap (regression) or influence function
(aipw).
}
\examples{
dat <- sim_mediation(2000, dgp = "front_door", seed = 1)
fit <- front_door(dat, "y", "d", "m", x = c("x1", "x2"), n_boot = 99, seed = 1)
fit
attr(dat, "truth")$total
}
\references{
Pearl, J. (1995). Causal diagrams for empirical research. \emph{Biometrika},
82(4), 669-688.

Bellemare, M. F., Bloem, J. R., and Wexler, N. (2020). The paper of how:
estimating treatment effects using the front-door criterion. Working paper.

Fulcher, I. R., Shpitser, I., Marealle, S., and Tchetgen Tchetgen, E. J.
(2020). Robust inference on population indirect causal effects: the
generalized front door criterion. \emph{JRSS-B}, 82(1), 199-214.
}
