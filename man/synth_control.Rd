% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synth-control.R
\name{synth_control}
\alias{synth_control}
\title{Synthetic control}
\usage{
synth_control(
  data,
  id,
  time,
  y,
  d,
  x = NULL,
  lags = "all",
  pre_window = NULL,
  v = c("equal", "mspe", "regression"),
  demean = FALSE,
  constraints = c("simplex", "nonnegative", "none"),
  zeta = 0,
  augment = c("none", "ridge"),
  lambda = NULL,
  treated_units = c("average", "separate"),
  horizon = NULL,
  weights = NULL,
  v_control = list(),
  standardize = NULL
)
}
\arguments{
\item{data}{A balanced long panel.}

\item{id, time, y}{Column names of the unit identifier, the period, and the
outcome.}

\item{d}{Column name of the treatment indicator (1 for treated units in
periods at or after adoption, 0 otherwise).}

\item{x}{Character vector of covariate columns used as predictors; each
is averaged over \code{pre_window}.}

\item{lags}{\code{"all"} (every pre-treatment period of the outcome is a
predictor; the Ferman-Pinto and \code{synthdid} choice) or a vector of
periods whose outcomes are predictors (Andersson 2019 uses 1989, 1980,
and 1970).}

\item{pre_window}{Periods over which covariates are averaged. Default:
all pre-treatment periods.}

\item{v}{Predictor importance weights. \code{"equal"} (default), \code{"mspe"}
(nested optimization: outer search over \code{V} minimizing the pre-treatment
mean squared prediction error of the outcome, inner simplex problem;
the default of the \code{Synth} package), \code{"regression"} (regression-based
weights, \code{Synth}'s starting value), or a numeric vector with one entry
per predictor. Predictors are standardized by their standard deviation
across units before weighting.}

\item{demean}{If \code{TRUE}, outcome lags enter as deviations from each
unit's pre-treatment mean and the counterfactual adds back an
intercept: the demeaned synthetic control of Ferman and Pinto (2021),
equivalent to the intercept of Doudchenko and Imbens (2016).}

\item{constraints}{\code{"simplex"} (weights nonnegative and summing to one),
\code{"nonnegative"}, or \code{"none"} (unconstrained least squares).}

\item{zeta}{Ridge penalty on the weights, on the scale of
\code{\link[=sdid_weights]{sdid_weights()}}: the objective adds \code{zeta^2 * K * sum(w^2)} with \code{K}
predictors.}

\item{augment}{\code{"none"} or \code{"ridge"}: add the bias correction
\verb{m(z_0) - sum_j w_j m(z_j)} from a ridge regression of each period's
outcome on the predictors, fitted on the donors.}

\item{lambda}{Ridge penalty of the augmentation model; chosen by
leave-one-donor-out cross-validation on post-treatment outcomes when
\code{NULL}.}

\item{treated_units}{\code{"average"} fits one synthetic control to the average
of the treated units (they must adopt in the same period);
\code{"separate"} fits one synthetic control per treated unit, using as
donors the units untreated throughout that unit's window, and averages
the gaps in event time. Staggered adoption is allowed in the second
mode.}

\item{horizon}{In the separate mode, the number of post-treatment periods
kept for each unit (default: all available).}

\item{weights}{An earlier \code{cm_synth} object whose donor weights are
applied to the outcome \code{y} without re-estimation (for example, the
synthetic GDP check of Andersson 2019).}

\item{v_control}{List with \code{maxit} (outer iterations, default 500) and
\code{starts} (number of random starting values in addition to the equal
and regression-based starts, default 0) for \code{v = "mspe"}.}

\item{standardize}{Divide each predictor by its standard deviation across
units before weighting, as the \code{Synth} package does. Default \code{TRUE}
when covariates are present and \code{FALSE} for outcome lags alone, so that
the plain synthetic control is the least-squares problem on raw
outcomes.}
}
\value{
An object of class \code{cm_synth} with \code{estimate} (average
post-treatment gap), \code{effects} (a data frame with the treated and
synthetic paths and the gap in every period), \code{weights} (donor
weights), \code{v}, \code{balance} (treated, synthetic, and donor-mean value of
each predictor, with a range check), \code{intercept}, \code{augmentation}
(the per-period correction when \code{augment = "ridge"}), \code{lambda},
\code{pre_rmspe}, \code{post_rmspe}, \code{concentration} (\code{l2}, the squared norm of
the weights of Ferman and Pinto, and \code{effective_donors = 1 / l2}), the
block dimensions, the block matrices, and the settings. In the separate
mode the object holds \code{units} (one \code{cm_synth} per treated unit),
\code{unit_table}, \code{event_gap}, and a jackknife \code{std.error} over treated
units.
}
\description{
\code{synth_control()} builds the synthetic control of Abadie and Gardeazabal
(2003) and Abadie, Diamond, and Hainmueller (2010) from a long panel: a
weighted average of untreated donor units, with weights on the simplex,
that reproduces the treated unit's pre-treatment predictors. The
predictors are outcome lags and pre-treatment averages of covariates,
each weighted by an importance weight \code{V}. Options give the demeaned
synthetic control of Ferman and Pinto (2021) (\code{demean = TRUE}), relaxed
constraints (Doudchenko and Imbens 2016), a ridge penalty on the weights,
the ridge-augmented estimator of Ben-Michael, Feller, and Rothstein (2021)
(\code{augment = "ridge"}), and one synthetic control per treated unit for
staggered adoption (\code{treated_units = "separate"}).
}
\details{
With \code{lags = "all"}, \code{demean = FALSE}, and \code{zeta = 0} the weights
equal those of \code{sdid_weights(estimator = "sc")}; with \code{demean = TRUE}
the average post-treatment gap equals the \code{"difp"} estimate of
\code{\link[=sdid_weights]{sdid_weights()}}. Weights are solved with \code{quadprog} when installed and
with the Frank-Wolfe routine of \code{\link[=sdid_weights]{sdid_weights()}} otherwise.
}
\examples{
dat <- sim_synth_panel(n_donors = 15, t_pre = 15, t_post = 5, effect = 2, seed = 1)
fit <- synth_control(dat, id = "id", time = "time", y = "y", d = "d")
fit
fit_dm <- synth_control(dat, id = "id", time = "time", y = "y", d = "d", demean = TRUE)
c(sc = fit$estimate, demeaned = fit_dm$estimate)
}
\references{
Abadie, A., Diamond, A., and Hainmueller, J. (2010). Synthetic
control methods for comparative case studies. \emph{JASA}, 105(490), 493-505.
Ferman, B. and Pinto, C. (2021). Synthetic controls with imperfect
pretreatment fit. \emph{Quantitative Economics}, 12(4), 1197-1221.
Ben-Michael, E., Feller, A., and Rothstein, J. (2021). The augmented
synthetic control method. \emph{JASA}, 116(536), 1789-1803.
Doudchenko, N. and Imbens, G. W. (2016). Balancing, regression,
difference-in-differences and synthetic control methods: a synthesis.
NBER Working Paper 22791.
}
