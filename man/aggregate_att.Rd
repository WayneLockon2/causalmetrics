% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-att.R
\name{aggregate_att}
\alias{aggregate_att}
\title{Aggregate group-time effects into summary parameters}
\usage{
aggregate_att(
  x,
  type = c("dynamic", "group", "calendar", "simple"),
  balance_e = NULL,
  min_e = -Inf,
  max_e = Inf,
  n_boot = NULL,
  boot_weights = NULL,
  conf_level = NULL,
  seed = NULL,
  na.rm = FALSE
)
}
\arguments{
\item{x}{An object returned by \code{\link[=att_gt]{att_gt()}} with \code{keep_inffunc = TRUE}.}

\item{type}{\code{"dynamic"} (default), \code{"group"}, \code{"calendar"}, or \code{"simple"}.}

\item{balance_e}{For \code{type = "dynamic"}: keep only groups observed for at
least \code{balance_e} periods after treatment, so every event time averages
the same set of groups.}

\item{min_e, max_e}{Event-time window for the dynamic aggregation; \code{max_e}
also caps the post-treatment cells used by the other types.}

\item{n_boot, boot_weights, conf_level, seed}{Bootstrap settings; default to
those of \code{x}.}

\item{na.rm}{Drop cells whose \code{ATT(g, t)} is missing.}
}
\value{
An object of class \code{cm_agg_att} with \code{overall} (estimate,
standard error, interval), \code{by} (one row per event time, group, or
period with pointwise interval and uniform band), the aggregation
weights, \code{crit_val}, and the influence functions.
}
\description{
\code{aggregate_att()} turns the \code{ATT(g, t)} cells of \code{\link[=att_gt]{att_gt()}} into the
summary parameters of Callaway and Sant'Anna (2021): an event-study curve
by length of exposure (\code{"dynamic"}), average effects by cohort
(\code{"group"}), by calendar period (\code{"calendar"}), or one weighted average of
all post-treatment cells (\code{"simple"}). Weights are the group shares among
treated units, and the influence functions carry the estimation error of
those shares, so standard errors and uniform bands are valid for the
aggregate.
}
\details{
The dynamic aggregate at event time \code{e} averages \code{ATT(g, g + e)}
over the groups observed at \code{e}, weighted by group size; its overall
value is the unweighted mean of the post-treatment event times. Group
aggregates average the post-treatment cells of each cohort with equal
weight over time; the overall value weights cohorts by size. Calendar
aggregates average the cohorts treated by each period. See Callaway
and Sant'Anna (2021, Section 3) for the definitions and the discussion
of compositional changes across event times, which \code{balance_e} removes.
}
\examples{
dat <- sim_did_panel(n_units = 300, n_periods = 8, seed = 1)
fit <- att_gt(dat, id = "id", time = "time", group = "g", y = "y", n_boot = 99)
aggregate_att(fit, type = "dynamic", min_e = -3, max_e = 3)
aggregate_att(fit, type = "group")
}
