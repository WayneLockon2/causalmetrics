% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gelbach-decomp.R
\name{gelbach_decomp}
\alias{gelbach_decomp}
\title{Gelbach decomposition of a coefficient change}
\usage{
gelbach_decomp(
  data,
  y,
  d,
  x_base = NULL,
  x_add,
  groups = NULL,
  fe = NULL,
  cluster = NULL,
  weights = NULL,
  n_boot = 499L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{y, d}{Outcome and treatment column names (the treatment may be
continuous).}

\item{x_base}{Covariates in the base regression.}

\item{x_add}{Covariates added in the full regression.}

\item{groups}{Optional named list assigning the added covariates to
groups whose contributions are summed.}

\item{fe}{Optional fixed-effect column names (regressions run with
\code{fixest::feols}).}

\item{cluster}{Optional cluster column for the bootstrap.}

\item{weights}{Optional column of regression weights (every regression,
including the auxiliary ones, is weighted; the identity still holds
exactly).}

\item{n_boot}{Bootstrap replications for the standard errors (0 skips
inference).}

\item{conf_level}{Confidence level.}

\item{seed}{Optional seed.}
}
\value{
A list of class \code{cm_gelbach} with \code{coefficients} (base, full, and
their difference), \code{contributions} (covariate or group, \code{Gamma},
\code{beta_full}, \code{contribution}, share of the change, and bootstrap
intervals), and the fitted models. \code{\link[=plot_gelbach]{plot_gelbach()}} draws the
contributions.
}
\description{
When covariates \code{x_add} are added to a base regression \code{Y ~ D + X_base},
the coefficient on \code{D} moves from \code{beta_base} to \code{beta_full}. Gelbach
(2016) shows that the change decomposes exactly, and independently of
the order in which covariates are added, as
\deqn{\hat\beta_{base} - \hat\beta_{full} = \sum_k \hat\Gamma_k \hat\beta_k^{full},}
where \code{Gamma_k} is the coefficient on \code{D} in the auxiliary regression of
the added covariate \code{k} on \code{D} and \code{X_base}, and \code{beta_k^full} is the
coefficient of covariate \code{k} in the full regression. Each term is the
part of the coefficient change attributable to covariate \code{k}. With
post-treatment covariates (candidate mediators) the decomposition is a
descriptive accounting of how much of the treatment coefficient the
mediators absorb, not an estimate of natural effects.
}
\examples{
dat <- sim_mediation(1000, dgp = "parallel", seed = 1)
g <- gelbach_decomp(dat, "y", "d", x_base = c("x1", "x2"), x_add = c("m1", "m2"), n_boot = 99)
g
}
\references{
Gelbach, J. B. (2016). When do covariates matter? And which ones, and how
much? \emph{Journal of Labor Economics}, 34(2), 509-543.
}
